# 데이터 전처리 및 파싱 아키텍처

> YouTube 여행 브이로그 데이터를 RAG 시스템에 활용하기 위한 ETL 파이프라인
>
> 작성일: 2025년 11월 7일

---

## 📋 목차

1. [개요](#개요)
2. [전체 데이터 흐름](#전체-데이터-흐름)
3. [데이터 소스 및 수집](#데이터-소스-및-수집)
4. [파싱 및 변환 프로세스](#파싱-및-변환-프로세스)
5. [임베딩 생성](#임베딩-생성)
6. [데이터베이스 저장](#데이터베이스-저장)
7. [품질 관리](#품질-관리)

---

## 개요

### 목적
YouTube 여행 브이로그의 비정형 데이터를 구조화하고 벡터 임베딩으로 변환하여 RAG 시스템에서 시맨틱 검색이 가능하도록 전처리

### 핵심 기술
- **데이터 소스**: YouTube Data API v3
- **파싱**: Python (re, json, BeautifulSoup)
- **임베딩**: OpenAI text-embedding-ada-002 (1536차원)
- **저장소**: PostgreSQL + pgvector
- **오케스트레이션**: Apache Airflow (예정) 또는 Django Management Commands

### 데이터 규모
- 예상 데이터: 10,000+ YouTube 여행 브이로그
- 평균 청크 크기: 480 토큰
- 총 벡터 크기: 10,000 x 1536차원

---

## 전체 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        데이터 전처리 파이프라인                             │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   1. 수집     │────▶│   2. 파싱     │────▶│  3. 임베딩    │────▶│  4. 저장      │
│   (Extract)  │     │  (Transform)  │     │  (Embed)      │     │   (Load)      │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
     │                     │                     │                     │
     ▼                     ▼                     ▼                     ▼
YouTube API          정규표현식            OpenAI API          PostgreSQL
JSON 데이터          텍스트 정제           Ada-002 모델         pgvector
메타데이터           구조화 변환            1536차원            인덱싱


              ┌────────────────────────────────┐
              │      품질 관리 레이어           │
              ├────────────────────────────────┤
              │ - 중복 제거 (video_id)         │
              │ - 데이터 검증 (필수 필드)       │
              │ - 에러 핸들링 및 로깅           │
              │ - 재시도 메커니즘               │
              └────────────────────────────────┘
```

---

## 데이터 소스 및 수집

### YouTube Data API v3 활용

```python
# 1단계: YouTube API를 통한 데이터 수집
┌────────────────────────────────────────────────────────────────┐
│                   YouTube API 요청                              │
└────────────────────────────────────────────────────────────────┘

검색 쿼리: "서울 여행", "제주도 3박4일", "일본 오사카 맛집" 등
필터:
  - 조회수 >= 1,000
  - 업로드 시간: 최근 2년
  - 언어: 한국어 (ko)
  - 재생목록 제외

API 응답 구조 (JSON):
{
  "video_id": "dQw4w9WgXcQ",
  "title": "서울 3박4일 완벽 여행 코스 🇰🇷 | 맛집부터 핫플까지",
  "channel": "여행러버TV",
  "description": "안녕하세요! 이번에 서울 3박4일 여행 다녀왔어요...",
  "published_at": "2024-10-15T10:30:00Z",
  "view_count": 125430,
  "duration": "PT15M32S",
  "tags": ["서울여행", "맛집추천", "핫플레이스"],
  "thumbnails": { "high": { "url": "..." } }
}
```

### 수집 전략

1. **키워드 기반 수집**
   - 국가명 + "여행", "여행코스", "브이로그"
   - 도시명 + "맛집", "관광지", "추천"

2. **채널 기반 수집**
   - 여행 전문 채널 구독자 추적
   - 인기 여행 브이로거 영상 크롤링

3. **재생목록 수집**
   - "여행 시리즈", "세계일주" 재생목록 탐색

### API 호출 최적화

```python
# Batch 처리로 API 호출 최소화
search_params = {
    "part": "snippet",
    "maxResults": 50,  # 한 번에 50개
    "type": "video",
    "q": query,
    "relevanceLanguage": "ko",
    "order": "viewCount"
}

# Rate Limiting 준수
# YouTube API: 10,000 quota/day
# 각 search: 100 quota
# 각 video details: 1 quota
# → 하루 약 90개 검색 + 9,900개 상세 정보 조회 가능
```

---

## 파싱 및 변환 프로세스

### 단계별 파싱 워크플로우

```
원본 데이터 (YouTube JSON)
        ↓
┌────────────────────────────────────────────────────────────────────────┐
│                       STEP 1: 메타데이터 추출                            │
└────────────────────────────────────────────────────────────────────────┘

필수 필드 추출:
✓ video_id       → 고유 식별자
✓ title          → 영상 제목
✓ channel        → 채널명
✓ description    → 영상 설명
✓ published_at   → 업로드 날짜 → upload_year, upload_month
✓ view_count     → 조회수 → views_num

선택 필드:
- tags           → 태그 배열
- duration       → 영상 길이
- thumbnails     → 썸네일 URL

        ↓
┌────────────────────────────────────────────────────────────────────────┐
│                   STEP 2: 위치 정보 파싱 및 매칭                         │
└────────────────────────────────────────────────────────────────────────┘

제목 + 설명에서 위치 추출:
  예: "서울 3박4일 여행" → "서울"
  예: "오사카 맛집 투어" → "오사카"

데이터베이스 매칭:
  1. Country 테이블 검색
     - country_name LIKE '%서울%'
     - country_code (FK) 설정

  2. Region1 테이블 검색
     - city_name LIKE '%서울%'
     - region1_idx (FK) 설정

  3. Region2 테이블 검색 (선택)
     - region2_name (구, 동 정보)
     - region2_idx (FK) 설정

매칭 로직:
  IF 제목에 도시명 포함:
    → Region1 우선 매칭
  ELIF 설명에 국가명만 있음:
    → Country만 매칭
  ELSE:
    → NULL (수동 검토 대상)

        ↓
┌────────────────────────────────────────────────────────────────────────┐
│                   STEP 3: 여행 일정(Schedules) 파싱                      │
└────────────────────────────────────────────────────────────────────────┘

Description에서 일정 추출 (정규표현식 활용):

패턴 1: "Day 1: 장소1 → 장소2 → 장소3"
  정규식: r'Day\s*(\d+)\s*[:：]\s*(.+?)(?=Day\s*\d+|$)'

패턴 2: "첫째날: 인천공항 → 호텔 → 명동"
  정규식: r'([첫둘셋넷]째?날)\s*[:：]\s*(.+?)(?=[첫둘셋넷]째?날|$)'

패턴 3: 타임스탬프 기반 "00:00 인천공항"
  정규식: r'(\d{1,2}:\d{2})\s+(.+?)(?=\d{1,2}:\d{2}|$)'

추출 예시:
description = '''
Day 1: 인천공항 → 서울역 호텔 체크인 → 명동 쇼핑 → 남산타워 야경
Day 2: 경복궁 → 광화문 → 북촌한옥마을 → 청계천
Day 3: 강남 쇼핑 → 코엑스 → 잠실 롯데월드
'''

파싱 결과:
schedules = [
  {
    "day": 1,
    "places": ["인천공항", "서울역 호텔", "명동", "남산타워"]
  },
  {
    "day": 2,
    "places": ["경복궁", "광화문", "북촌한옥마을", "청계천"]
  },
  {
    "day": 3,
    "places": ["강남", "코엑스", "잠실 롯데월드"]
  }
]

        ↓
┌────────────────────────────────────────────────────────────────────────┐
│                    STEP 4: 텍스트 정제 및 정규화                         │
└────────────────────────────────────────────────────────────────────────┘

1. HTML 태그 제거
   BeautifulSoup.get_text()

2. 특수문자 정리
   - 이모지 제거: re.sub(r'[^\w\s가-힣]', '', text)
   - 연속 공백 제거: re.sub(r'\s+', ' ', text)

3. 텍스트 길이 제한
   - title: 최대 500자
   - description: 최대 5,000자

4. 언어 감지
   - 한국어(ko) 우선
   - 영어(en) 보조
   - 기타 언어는 제외

        ↓
┌────────────────────────────────────────────────────────────────────────┐
│                   STEP 5: JSON 메타데이터 구성                           │
└────────────────────────────────────────────────────────────────────────┘

최종 metadata JSON 구조:
{
  "description_ko": "한국어 설명 전체",
  "description_en": "영어 설명 (있는 경우)",
  "schedules": [
    {"day": 1, "places": ["장소1", "장소2"]},
    {"day": 2, "places": ["장소3", "장소4"]}
  ],
  "tags": ["서울여행", "맛집", "핫플"],
  "duration": "15:32",
  "thumbnail_url": "https://i.ytimg.com/...",
  "original_published_at": "2024-10-15T10:30:00Z"
}
```

### 코드 예시: 파싱 함수

```python
import re
from datetime import datetime
from typing import Dict, List, Optional

def parse_youtube_vlog(youtube_data: Dict) -> Dict:
    """
    YouTube API 응답을 TripCourseEmbedding 모델 형식으로 파싱

    Args:
        youtube_data: YouTube API JSON 응답

    Returns:
        파싱된 데이터 딕셔너리
    """

    # 1. 기본 메타데이터 추출
    video_id = youtube_data['id']
    title = clean_text(youtube_data['snippet']['title'])
    channel = youtube_data['snippet']['channelTitle']
    description = youtube_data['snippet']['description']

    # 2. 날짜 파싱
    published_at = datetime.fromisoformat(
        youtube_data['snippet']['publishedAt'].replace('Z', '+00:00')
    )
    upload_year = published_at.year
    upload_month = published_at.month

    # 3. 조회수 (statistics는 별도 API 호출 필요)
    views_num = int(youtube_data.get('statistics', {}).get('viewCount', 0))

    # 4. 위치 정보 추출
    location_info = extract_location(title, description)

    # 5. 일정 파싱
    schedules = parse_schedules(description)

    # 6. metadata JSON 구성
    metadata = {
        "description_ko": description,
        "schedules": schedules,
        "tags": youtube_data['snippet'].get('tags', []),
        "duration": youtube_data.get('contentDetails', {}).get('duration'),
        "thumbnail_url": youtube_data['snippet']['thumbnails']['high']['url'],
        "original_published_at": published_at.isoformat()
    }

    return {
        "video_id": video_id,
        "title": title,
        "channel": channel,
        "url": f"https://www.youtube.com/watch?v={video_id}",
        "country_name": location_info.get('country'),
        "country_code": location_info.get('country_code'),
        "region1_idx": location_info.get('region1_idx'),
        "upload_year": upload_year,
        "upload_month": upload_month,
        "views_num": views_num,
        "metadata": metadata
    }


def parse_schedules(description: str) -> List[Dict]:
    """
    영상 설명에서 여행 일정 추출

    패턴:
    - "Day 1: 장소1 → 장소2"
    - "첫째날: 장소1, 장소2"
    - "1일차 - 장소1 / 장소2"
    """
    schedules = []

    # 패턴 1: Day N
    day_pattern = r'Day\s*(\d+)\s*[:：]\s*(.+?)(?=Day\s*\d+|$)'
    matches = re.finditer(day_pattern, description, re.IGNORECASE | re.MULTILINE)

    for match in matches:
        day_num = int(match.group(1))
        places_str = match.group(2).strip()

        # 장소 구분자: →, -, /, ,
        places = re.split(r'[→\-/,]\s*', places_str)
        places = [p.strip() for p in places if p.strip()]

        schedules.append({
            "day": day_num,
            "places": places[:10]  # 최대 10개 장소
        })

    # 패턴 2: 한글 (첫째날, 둘째날...)
    if not schedules:
        kr_day_pattern = r'([첫둘셋넷다섯]째?날)\s*[:：]\s*(.+?)(?=[첫둘셋넷다섯]째?날|$)'
        matches = re.finditer(kr_day_pattern, description, re.MULTILINE)

        day_map = {"첫": 1, "둘": 2, "셋": 3, "넷": 4, "다섯": 5}

        for match in matches:
            day_str = match.group(1)
            day_num = day_map.get(day_str[0], 0)
            places_str = match.group(2).strip()

            places = re.split(r'[→\-/,]\s*', places_str)
            places = [p.strip() for p in places if p.strip()]

            schedules.append({
                "day": day_num,
                "places": places[:10]
            })

    return schedules


def extract_location(title: str, description: str) -> Dict:
    """
    제목과 설명에서 위치 정보 추출 및 DB 매칭

    Returns:
        {
            "country": "대한민국",
            "country_code": Country FK,
            "region1_idx": Region1 FK
        }
    """
    from apps.common.models import Country, Region1

    # 주요 도시 키워드
    cities = ["서울", "부산", "제주", "인천", "대구", "광주", "대전",
              "도쿄", "오사카", "교토", "후쿠오카", "삿포로",
              "파리", "런던", "뉴욕", "로스앤젤레스", "샌프란시스코"]

    # 제목에서 도시 검색
    for city in cities:
        if city in title or city in description[:200]:
            # DB에서 Region1 검색
            region1 = Region1.objects.filter(
                city_name__icontains=city
            ).first()

            if region1:
                return {
                    "country": region1.country_code.country_name,
                    "country_code": region1.country_code,
                    "region1_idx": region1
                }

    # 국가명만 검색
    countries = ["한국", "일본", "미국", "프랑스", "영국", "이탈리아"]
    for country_name in countries:
        if country_name in title or country_name in description[:200]:
            country = Country.objects.filter(
                country_name__icontains=country_name
            ).first()

            if country:
                return {
                    "country": country.country_name,
                    "country_code": country,
                    "region1_idx": None
                }

    return {
        "country": None,
        "country_code": None,
        "region1_idx": None
    }


def clean_text(text: str) -> str:
    """텍스트 정제"""
    # HTML 태그 제거 (간단 버전)
    text = re.sub(r'<[^>]+>', '', text)

    # 연속 공백 제거
    text = re.sub(r'\s+', ' ', text)

    # 양쪽 공백 제거
    text = text.strip()

    return text
```

---

## 임베딩 생성

### OpenAI Embeddings API 활용

```python
┌────────────────────────────────────────────────────────────────────────┐
│                      임베딩 생성 프로세스                                 │
└────────────────────────────────────────────────────────────────────────┘

STEP 1: 임베딩용 텍스트 구성
─────────────────────────────

여러 필드를 결합하여 하나의 통합 텍스트 생성:

combined_text = f"""
제목: {title}
국가: {country_name}
도시: {city_name}
설명: {description[:500]}  # 처음 500자만
일정: {format_schedules(schedules)}
"""

예시:
"""
제목: 서울 3박4일 완벽 여행 코스
국가: 대한민국
도시: 서울
설명: 안녕하세요! 이번에 서울 3박4일 여행 다녀왔어요.
      경복궁, 남산타워, 명동 쇼핑까지 알찬 코스로 다녀왔습니다...
일정: Day 1: 인천공항 → 호텔 → 명동 → 남산타워
     Day 2: 경복궁 → 북촌한옥마을 → 청계천
     Day 3: 강남 → 코엑스 → 잠실
"""

평균 토큰 수: 480 토큰
최대 토큰 제한: 8,191 토큰 (Ada-002 모델)


STEP 2: OpenAI API 호출
──────────────────────

import openai

response = openai.embeddings.create(
    model="text-embedding-ada-002",
    input=combined_text
)

embedding = response.data[0].embedding
# → List[float] with 1536 dimensions


STEP 3: 벡터 정규화 (선택)
─────────────────────────

Ada-002는 이미 정규화된 벡터 반환 (L2 norm = 1)
추가 정규화 불필요

import numpy as np
embedding_array = np.array(embedding)
norm = np.linalg.norm(embedding_array)
# norm ≈ 1.0 (이미 정규화됨)


STEP 4: 배치 처리 최적화
────────────────────────

한 번에 여러 문서 임베딩 (최대 2,048개):

texts = [combined_text1, combined_text2, ...]
response = openai.embeddings.create(
    model="text-embedding-ada-002",
    input=texts[:100]  # 100개씩 배치
)

embeddings = [item.embedding for item in response.data]

성능:
- 단일 요청: ~200ms
- 배치 100개: ~2초 (개당 20ms)
- 비용: $0.0001 / 1K 토큰
```

### 임베딩 품질 최적화

```
품질 향상 전략:
────────────────

1. 컨텍스트 강화
   ✓ 제목 + 설명 + 일정 결합
   ✓ 위치 정보 명시적 포함
   ✗ 너무 긴 텍스트 (8K 토큰 초과)

2. 토큰 수 최적화
   목표: 400-600 토큰
   - 너무 짧음 (<200): 정보 부족
   - 너무 김 (>2000): 노이즈 증가

3. 언어 일관성
   한국어와 영어 혼재 시:
   - 한국어 우선
   - 주요 키워드는 양쪽 언어 포함

4. 시맨틱 의미 강화
   예: "서울 여행" vs "서울 3박4일 맛집 투어"
   → 후자가 더 구체적이고 검색 품질 향상
```

---

## 데이터베이스 저장

### TripCourseEmbedding 모델 구조

```python
┌────────────────────────────────────────────────────────────────────────┐
│                  TripCourseEmbedding 테이블 구조                         │
└────────────────────────────────────────────────────────────────────────┘

class TripCourseEmbedding(models.Model):
    # 고유 식별자
    video_id = CharField(max_length=50, unique=True, db_index=True)

    # 기본 메타데이터
    title = TextField()
    channel = CharField(max_length=255)
    url = TextField()

    # 위치 정보 (FK)
    country_name = CharField(max_length=100)  # 비정규화 (검색 속도)
    country_code = ForeignKey('common.Country')
    region1_idx = ForeignKey('common.Region1')  # 도시
    region2_idx = ForeignKey('common.Region2')  # 구/동

    # 업로드 정보
    upload_year = IntegerField()
    upload_month = IntegerField()
    views_num = IntegerField()

    # 벡터 임베딩 (핵심!)
    content_embedding = VectorField(dimensions=1536)  # pgvector

    # 원본 데이터 (JSON)
    metadata = JSONField()  # schedules, description, tags 등

    # 시스템 필드
    created_at = DateTimeField(auto_now_add=True)
    updated_at = DateTimeField(auto_now=True)


인덱스:
───────
1. video_id (UNIQUE) - 중복 방지
2. (country_code, region1_idx) - 위치 필터링
3. (upload_year, upload_month) - 시간 필터링
4. views_num DESC - 인기순 정렬
5. content_embedding (IVFFlat) - 벡터 유사도 검색
```

### 저장 프로세스

```python
from apps.ai.models import TripCourseEmbedding
from apps.common.models import Country, Region1
from django.db import transaction
import openai

def save_vlog_to_db(parsed_data: Dict) -> TripCourseEmbedding:
    """
    파싱된 데이터를 데이터베이스에 저장

    Args:
        parsed_data: parse_youtube_vlog() 결과

    Returns:
        저장된 TripCourseEmbedding 인스턴스
    """

    # 1. 임베딩 생성
    combined_text = create_combined_text(parsed_data)
    embedding = create_embedding(combined_text)

    # 2. 트랜잭션 내에서 저장
    with transaction.atomic():
        trip_embedding, created = TripCourseEmbedding.objects.update_or_create(
            video_id=parsed_data['video_id'],
            defaults={
                'title': parsed_data['title'],
                'channel': parsed_data['channel'],
                'url': parsed_data['url'],
                'country_name': parsed_data['country_name'],
                'country_code': parsed_data['country_code'],
                'region1_idx': parsed_data['region1_idx'],
                'upload_year': parsed_data['upload_year'],
                'upload_month': parsed_data['upload_month'],
                'views_num': parsed_data['views_num'],
                'content_embedding': embedding,  # 1536차원 벡터
                'metadata': parsed_data['metadata']
            }
        )

    action = "생성" if created else "업데이트"
    print(f"✅ {action}: {parsed_data['title']} ({parsed_data['video_id']})")

    return trip_embedding


def create_combined_text(parsed_data: Dict) -> str:
    """임베딩용 통합 텍스트 생성"""
    parts = [
        f"제목: {parsed_data['title']}",
    ]

    if parsed_data['country_name']:
        parts.append(f"국가: {parsed_data['country_name']}")

    if parsed_data.get('region1_idx'):
        parts.append(f"도시: {parsed_data['region1_idx'].city_name}")

    # 설명 (처음 500자)
    description = parsed_data['metadata'].get('description_ko', '')
    if description:
        parts.append(f"설명: {description[:500]}")

    # 일정
    schedules = parsed_data['metadata'].get('schedules', [])
    if schedules:
        schedule_text = format_schedules(schedules)
        parts.append(f"일정:\n{schedule_text}")

    return "\n\n".join(parts)


def format_schedules(schedules: List[Dict]) -> str:
    """일정을 읽기 쉬운 텍스트로 포맷팅"""
    lines = []
    for schedule in schedules:
        day = schedule['day']
        places = schedule['places']
        places_str = " → ".join(places)
        lines.append(f"Day {day}: {places_str}")
    return "\n".join(lines)


def create_embedding(text: str) -> List[float]:
    """OpenAI API로 임베딩 생성"""
    response = openai.embeddings.create(
        model="text-embedding-ada-002",
        input=text
    )
    return response.data[0].embedding
```

### 배치 저장 최적화

```python
def bulk_save_vlogs(parsed_data_list: List[Dict], batch_size: int = 50):
    """
    대량 데이터 배치 저장

    Args:
        parsed_data_list: 파싱된 데이터 리스트
        batch_size: 배치 크기
    """

    total = len(parsed_data_list)
    saved = 0
    errors = 0

    for i in range(0, total, batch_size):
        batch = parsed_data_list[i:i+batch_size]

        # 1. 배치 임베딩 생성 (한 번에 여러 개)
        combined_texts = [create_combined_text(data) for data in batch]

        try:
            response = openai.embeddings.create(
                model="text-embedding-ada-002",
                input=combined_texts
            )
            embeddings = [item.embedding for item in response.data]

            # 2. 트랜잭션 내에서 배치 저장
            with transaction.atomic():
                for data, embedding in zip(batch, embeddings):
                    try:
                        save_vlog_to_db_with_embedding(data, embedding)
                        saved += 1
                    except Exception as e:
                        print(f"❌ 저장 실패: {data['video_id']} - {e}")
                        errors += 1

            print(f"📊 진행률: {i+len(batch)}/{total} ({saved} 성공, {errors} 실패)")

        except Exception as e:
            print(f"❌ 배치 임베딩 실패: {e}")
            errors += len(batch)

    print(f"\n{'='*50}")
    print(f"✅ 저장 완료: {saved}개 성공, {errors}개 실패")
    print(f"{'='*50}")
```

---

## 품질 관리

### 데이터 검증

```python
┌────────────────────────────────────────────────────────────────────────┐
│                       데이터 품질 검증 체크리스트                          │
└────────────────────────────────────────────────────────────────────────┘

def validate_parsed_data(data: Dict) -> tuple[bool, str]:
    """
    파싱된 데이터 검증

    Returns:
        (is_valid, error_message)
    """

    # 1. 필수 필드 존재 여부
    required_fields = ['video_id', 'title', 'url']
    for field in required_fields:
        if not data.get(field):
            return False, f"필수 필드 누락: {field}"

    # 2. video_id 형식 검증 (YouTube ID는 11자)
    if len(data['video_id']) != 11:
        return False, f"잘못된 video_id 형식: {data['video_id']}"

    # 3. 제목 길이 검증
    if len(data['title']) > 500:
        return False, "제목이 너무 김 (>500자)"

    # 4. 조회수 검증
    if data.get('views_num', 0) < 0:
        return False, "조회수는 음수일 수 없음"

    # 5. 날짜 검증
    if data.get('upload_year'):
        if not (2000 <= data['upload_year'] <= 2030):
            return False, f"비정상 업로드 연도: {data['upload_year']}"

    # 6. metadata 구조 검증
    if 'metadata' in data:
        if not isinstance(data['metadata'], dict):
            return False, "metadata는 딕셔너리여야 함"

        # schedules 구조 검증
        schedules = data['metadata'].get('schedules', [])
        if schedules:
            for schedule in schedules:
                if 'day' not in schedule or 'places' not in schedule:
                    return False, "schedules 구조 오류"

    return True, ""


# 검증 예시
is_valid, error = validate_parsed_data(parsed_data)
if not is_valid:
    print(f"❌ 검증 실패: {error}")
    # 에러 로깅 및 재시도 로직
else:
    save_vlog_to_db(parsed_data)
```

### 중복 제거

```python
┌────────────────────────────────────────────────────────────────────────┐
│                          중복 제거 전략                                   │
└────────────────────────────────────────────────────────────────────────┘

1. video_id 기반 중복 방지
   - UNIQUE 제약조건 (DB 레벨)
   - update_or_create() 사용 (Django ORM)

2. 재업로드 영상 감지
   - 제목 유사도 검사 (Levenshtein distance)
   - 같은 채널에서 비슷한 제목 → 수동 검토

3. 중복 처리 로직
   IF video_id 존재:
     IF metadata 업데이트됨 OR 조회수 증가:
       → 업데이트
     ELSE:
       → 스킵
   ELSE:
     → 신규 생성

코드:
trip, created = TripCourseEmbedding.objects.update_or_create(
    video_id=video_id,
    defaults={...}
)

if created:
    print(f"✅ 신규 생성: {video_id}")
else:
    print(f"🔄 업데이트: {video_id}")
```

### 에러 핸들링

```python
┌────────────────────────────────────────────────────────────────────────┐
│                        에러 핸들링 및 재시도                              │
└────────────────────────────────────────────────────────────────────────┘

import logging
from tenacity import retry, stop_after_attempt, wait_exponential

logger = logging.getLogger(__name__)


@retry(
    stop=stop_after_attempt(3),  # 최대 3번 재시도
    wait=wait_exponential(multiplier=1, min=2, max=10),  # 지수 백오프
    reraise=True
)
def create_embedding_with_retry(text: str) -> List[float]:
    """
    재시도 로직이 포함된 임베딩 생성

    OpenAI API 일시적 오류 대응:
    - Rate Limit (429)
    - Timeout (504)
    - Server Error (500, 502, 503)
    """
    try:
        response = openai.embeddings.create(
            model="text-embedding-ada-002",
            input=text
        )
        return response.data[0].embedding

    except openai.RateLimitError as e:
        logger.warning(f"⚠️ Rate Limit 도달, 재시도 중... {e}")
        raise  # tenacity가 재시도

    except openai.APITimeoutError as e:
        logger.warning(f"⚠️ API Timeout, 재시도 중... {e}")
        raise

    except openai.APIError as e:
        logger.error(f"❌ OpenAI API 오류: {e}")
        raise

    except Exception as e:
        logger.error(f"❌ 알 수 없는 오류: {e}", exc_info=True)
        raise


def safe_save_vlog(parsed_data: Dict) -> Optional[TripCourseEmbedding]:
    """
    안전한 저장 (에러 핸들링 포함)
    """
    try:
        # 1. 검증
        is_valid, error = validate_parsed_data(parsed_data)
        if not is_valid:
            logger.error(f"❌ 검증 실패: {error} - {parsed_data['video_id']}")
            return None

        # 2. 임베딩 생성 (재시도 포함)
        combined_text = create_combined_text(parsed_data)
        embedding = create_embedding_with_retry(combined_text)

        # 3. DB 저장
        trip = save_vlog_to_db_with_embedding(parsed_data, embedding)
        logger.info(f"✅ 저장 성공: {parsed_data['video_id']}")
        return trip

    except Exception as e:
        logger.error(
            f"❌ 저장 실패: {parsed_data['video_id']} - {e}",
            exc_info=True
        )
        return None


# 실패한 항목 추적
failed_items = []

for data in parsed_data_list:
    result = safe_save_vlog(data)
    if result is None:
        failed_items.append(data['video_id'])

# 실패 항목 로깅
if failed_items:
    logger.warning(f"⚠️ {len(failed_items)}개 항목 저장 실패: {failed_items}")
```

### 모니터링 및 로깅

```python
┌────────────────────────────────────────────────────────────────────────┐
│                      데이터 파이프라인 모니터링                            │
└────────────────────────────────────────────────────────────────────────┘

import logging
from datetime import datetime

# 로깅 설정
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s [%(levelname)s] %(message)s',
    handlers=[
        logging.FileHandler('/app/logs/data_pipeline.log'),
        logging.StreamHandler()
    ]
)

logger = logging.getLogger(__name__)


class PipelineStats:
    """파이프라인 통계"""

    def __init__(self):
        self.start_time = datetime.now()
        self.total = 0
        self.success = 0
        self.failed = 0
        self.skipped = 0
        self.api_calls = 0
        self.api_cost = 0.0

    def log_summary(self):
        duration = (datetime.now() - self.start_time).total_seconds()

        logger.info("="*60)
        logger.info("파이프라인 실행 요약")
        logger.info("="*60)
        logger.info(f"총 처리: {self.total}개")
        logger.info(f"✅ 성공: {self.success}개 ({self.success/self.total*100:.1f}%)")
        logger.info(f"❌ 실패: {self.failed}개 ({self.failed/self.total*100:.1f}%)")
        logger.info(f"⏭️  스킵: {self.skipped}개")
        logger.info(f"⏱️  소요 시간: {duration:.1f}초")
        logger.info(f"🚀 처리 속도: {self.total/duration:.2f}개/초")
        logger.info(f"💰 API 비용: ${self.api_cost:.4f}")
        logger.info("="*60)


# 사용 예시
stats = PipelineStats()

for data in youtube_data_list:
    stats.total += 1

    try:
        parsed = parse_youtube_vlog(data)
        result = safe_save_vlog(parsed)

        if result:
            stats.success += 1
            stats.api_calls += 1
            stats.api_cost += 0.0001 * (480 / 1000)  # 평균 480 토큰
        else:
            stats.failed += 1

    except Exception as e:
        stats.failed += 1
        logger.error(f"처리 실패: {e}")

stats.log_summary()
```

---

## 전체 워크플로우 요약

```
┌──────────────────────────────────────────────────────────────────────────┐
│                    데이터 전처리 전체 워크플로우                              │
└──────────────────────────────────────────────────────────────────────────┘

1. 데이터 수집 (YouTube API)
   ├─ 검색 쿼리 실행 (국가/도시 + "여행")
   ├─ 영상 리스트 획득
   └─ 상세 정보 조회 (title, description, stats)
         ↓
2. 파싱 및 변환
   ├─ 메타데이터 추출 (video_id, title, channel, etc.)
   ├─ 위치 정보 파싱 및 DB 매칭
   │   ├─ Country 매칭
   │   ├─ Region1 (도시) 매칭
   │   └─ Region2 (구/동) 매칭
   ├─ 여행 일정(Schedules) 추출
   │   └─ 정규표현식으로 Day 1, Day 2 등 파싱
   ├─ 텍스트 정제 (HTML 태그, 특수문자 제거)
   └─ JSON metadata 구성
         ↓
3. 데이터 검증
   ├─ 필수 필드 체크
   ├─ 형식 검증 (video_id, 날짜, 조회수)
   ├─ 중복 체크 (video_id UNIQUE)
   └─ 품질 필터링 (최소 조회수, 언어)
         ↓
4. 임베딩 생성
   ├─ 통합 텍스트 구성
   │   └─ title + country + city + description + schedules
   ├─ OpenAI Embeddings API 호출
   │   └─ text-embedding-ada-002 (1536차원)
   └─ 배치 처리 (100개씩)
         ↓
5. 데이터베이스 저장
   ├─ TripCourseEmbedding 모델에 저장
   │   ├─ video_id (PK)
   │   ├─ 메타데이터 (title, channel, url, etc.)
   │   ├─ 위치 정보 (country_code, region1_idx FK)
   │   ├─ content_embedding (VectorField)
   │   └─ metadata (JSONField)
   ├─ 인덱스 생성
   │   ├─ video_id (UNIQUE)
   │   ├─ (country_code, region1_idx)
   │   └─ content_embedding (IVFFlat)
   └─ 트랜잭션 처리
         ↓
6. 품질 관리
   ├─ 에러 로깅
   ├─ 재시도 메커니즘 (Rate Limit 대응)
   ├─ 통계 수집 (성공/실패/스킵)
   └─ 모니터링 대시보드
         ↓
7. RAG 시스템 활용 준비 완료! ✅
```

---

## 성능 지표

### 처리 속도

```
┌────────────────────────────────────────────────────────────────┐
│                   파이프라인 성능 벤치마크                        │
└────────────────────────────────────────────────────────────────┘

단계별 소요 시간 (영상 1개 기준):
├─ YouTube API 호출: ~200ms
├─ 파싱 및 변환: ~50ms
├─ 데이터 검증: ~10ms
├─ 임베딩 생성: ~200ms (OpenAI API)
└─ DB 저장: ~30ms
   ────────────────────
   총 소요 시간: ~490ms (약 0.5초)

배치 처리 (100개):
├─ YouTube API 호출: ~5초 (배치 50개씩 2번)
├─ 파싱: ~5초
├─ 임베딩 생성: ~4초 (배치 100개)
└─ DB 저장: ~3초
   ────────────────────
   총 소요 시간: ~17초

   → 100개/17초 = 5.88개/초
   → 시간당 약 21,000개 처리 가능

일일 처리량:
├─ YouTube API 제한: 10,000 quota/day
│   └─ 약 90개 검색 + 9,000개 상세 조회
├─ OpenAI API 제한: 3,000 requests/min
│   └─ 충분함 (배치 처리로 요청 최소화)
└─ 예상 일일 처리량: ~9,000개 영상
```

### 비용 분석

```
┌────────────────────────────────────────────────────────────────┐
│                        API 비용 분석                            │
└────────────────────────────────────────────────────────────────┘

OpenAI Embeddings API:
├─ 모델: text-embedding-ada-002
├─ 가격: $0.0001 / 1K 토큰
├─ 평균 토큰/문서: 480 토큰
└─ 문서당 비용: $0.000048 (약 0.005원)

YouTube Data API:
├─ 무료 쿼터: 10,000 quota/day
├─ 초과 시: $0.50 / 1,000 quota
└─ 일일 비용: 무료 (쿼터 내 사용)

총 비용 (10,000개 영상 처리):
├─ OpenAI: $0.48
├─ YouTube: $0 (무료 쿼터)
└─ 총합: $0.48 (약 650원)

월간 비용 (30일 x 9,000개):
└─ OpenAI: ~$13 (약 17,000원)
   └─ 매우 저렴!
```

---

## 결론

이 데이터 전처리 파이프라인은 YouTube 여행 브이로그의 비정형 데이터를:

1. ✅ **수집** - YouTube API로 대량 데이터 획득
2. ✅ **파싱** - 정규표현식으로 일정/위치 추출
3. ✅ **구조화** - DB 모델에 맞게 변환
4. ✅ **벡터화** - OpenAI Embeddings로 1536차원 변환
5. ✅ **저장** - PostgreSQL + pgvector에 효율적 저장

이를 통해 **RAG 시스템에서 시맨틱 검색**이 가능해지며,
사용자 쿼리에 대해 **관련성 높은 여행 코스를 추천**할 수 있습니다!

---

**다음 단계**: Airflow DAG 구성 또는 Django Management Command로 자동화
