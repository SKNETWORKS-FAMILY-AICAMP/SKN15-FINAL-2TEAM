# 웹 브라우저 마이크 권한 및 음성인식 문제 해결 가이드

## 목차
1. [개요](#개요)
2. [시스템 구조](#시스템-구조)
3. [브라우저별 마이크 권한 설정](#브라우저별-마이크-권한-설정)
4. [자주 발생하는 문제와 해결방법](#자주-발생하는-문제와-해결방법)
5. [에러 코드별 해결방법](#에러-코드별-해결방법)
6. [HTTPS 요구사항](#https-요구사항)
7. [디버깅 방법](#디버깅-방법)
8. [사용자 가이드](#사용자-가이드)

---

## 개요

Triplan 프로젝트는 2가지 음성 인식 방식을 제공합니다:

| 방식 | 설명 | 사용 Hook | 비고 |
|------|------|-----------|------|
| **Web Speech API** | 브라우저 내장 음성인식 (Google) | `useVoiceRecognition.ts` | 실시간, 무료, Chrome/Edge 전용 |
| **Local STT Model** | 자체 학습 모델 (서버) | `useVoiceRecognitionLocal.ts` | 녹음 후 전송, 모든 브라우저 |

두 방식 모두 **마이크 권한**이 필요하며, 보안상 **HTTPS 환경**에서만 동작합니다.

---

## 시스템 구조

### 1. Web Speech API (useVoiceRecognition.ts)

```
사용자 음성
    ↓
브라우저 마이크 권한 요청 (getUserMedia 암묵적)
    ↓
Web Speech API (SpeechRecognition)
    ↓ (실시간 스트리밍)
Google 음성인식 서버
    ↓
실시간 텍스트 결과 (interim + final)
    ↓
React 컴포넌트 (채팅 입력)
```

**코드 위치:** `frontend/src/hooks/useVoiceRecognition.ts`

**특징:**
- ✅ 실시간 인식 (중간 결과 표시)
- ✅ 무료 (Google 제공)
- ✅ 네트워크 지연 최소
- ❌ Chrome/Edge만 지원 (Safari, Firefox 미지원)
- ❌ 인터넷 필수

---

### 2. Local STT Model (useVoiceRecognitionLocal.ts)

```
사용자 음성
    ↓
브라우저 마이크 권한 요청 (getUserMedia 명시적)
    ↓
MediaRecorder로 녹음 (audio/webm)
    ↓ (녹음 완료 후)
Backend STT API (/api/chat/stt/transcribe/)
    ↓
자체 학습 model.safetensors 처리
    ↓
텍스트 결과 반환
    ↓
React 컴포넌트 (채팅 입력)
```

**코드 위치:**
- `frontend/src/hooks/useVoiceRecognitionLocal.ts`
- `frontend/src/hooks/useAudioRecorder.ts`

**특징:**
- ✅ 모든 모던 브라우저 지원
- ✅ 오프라인 가능 (모델 서버만 필요)
- ✅ 한국어 특화 학습 가능
- ❌ 녹음 후 전송 (실시간 아님)
- ❌ 서버 리소스 필요

---

## 브라우저별 마이크 권한 설정

### Chrome (권장)

#### ✅ 권한 허용 방법

1. **주소창 왼쪽 자물쇠 아이콘** 클릭
2. **사이트 설정** 클릭
3. **마이크** 항목에서 **허용** 선택
4. 페이지 새로고침

#### 🔧 권한 초기화 (거부했을 때)

```
Chrome 설정 → 개인정보 및 보안 → 사이트 설정 → 마이크
→ 차단된 사이트 목록에서 제거
```

또는:

```
주소창에 입력: chrome://settings/content/microphone
→ 차단 목록에서 localhost:3000 제거
```

#### 💡 권한 확인 방법

```javascript
// 개발자 도구 Console에서 실행
navigator.permissions.query({ name: 'microphone' }).then(result => {
  console.log('Microphone permission:', result.state);
  // "granted" = 허용
  // "denied" = 거부
  // "prompt" = 묻지 않음 (첫 요청 시)
});
```

---

### Edge

Chrome과 동일한 방식 (Chromium 기반)

1. 주소창 자물쇠 → 사이트 권한
2. 마이크 → 허용
3. 페이지 새로고침

---

### Firefox

#### ✅ 권한 허용 방법

1. **주소창 왼쪽 자물쇠 아이콘** 클릭
2. **권한** → **마이크 사용** 허용
3. 페이지 새로고침

#### 🔧 권한 초기화

```
Firefox 설정 → 개인정보 및 보안 → 권한 → 마이크 → 설정
→ localhost:3000 제거
```

또는:

```
주소창에 입력: about:preferences#privacy
→ 권한 → 마이크 → 설정 → localhost 제거
```

#### ⚠️ Web Speech API 미지원

Firefox는 `SpeechRecognition` API를 지원하지 않습니다.
**→ Local STT Model (useVoiceRecognitionLocal) 사용 필수**

---

### Safari (macOS/iOS)

#### ✅ 권한 허용 방법 (macOS)

1. Safari → **환경설정** (⌘,)
2. **웹사이트** 탭
3. **마이크** → localhost 항목 **허용**
4. 페이지 새로고침

#### ✅ 권한 허용 방법 (iOS)

1. iOS **설정** → **Safari**
2. **마이크** 켜기
3. Safari 재시작

#### ⚠️ Web Speech API 미지원

Safari도 `SpeechRecognition` API를 지원하지 않습니다.
**→ Local STT Model (useVoiceRecognitionLocal) 사용 필수**

---

## 자주 발생하는 문제와 해결방법

### 문제 1: "마이크 권한이 거부되었습니다"

#### 증상
```
Error: NotAllowedError: Permission denied
또는
"마이크 권한이 거부되었습니다. 브라우저 설정에서 마이크 권한을 허용해주세요."
```

#### 원인
- 사용자가 권한 요청 팝업에서 **차단** 클릭
- 브라우저 설정에서 마이크가 차단됨

#### 해결방법

**Chrome/Edge:**
```
1. chrome://settings/content/microphone
2. 차단 목록에서 localhost:3000 제거
3. 페이지 새로고침
4. 마이크 버튼 다시 클릭 → "허용" 선택
```

**Firefox:**
```
1. about:preferences#privacy
2. 권한 → 마이크 → 설정
3. localhost:3000 제거
4. 페이지 새로고침
```

**Safari:**
```
1. Safari → 환경설정 → 웹사이트 → 마이크
2. localhost 항목 "거부" → "허용"으로 변경
3. 페이지 새로고침
```

---

### 문제 2: "마이크를 찾을 수 없습니다"

#### 증상
```
Error: NotFoundError: Requested device not found
또는
"마이크를 찾을 수 없습니다. 마이크를 연결해주세요."
```

#### 원인
- 물리적 마이크가 연결되지 않음
- 마이크가 다른 앱에서 사용 중
- OS 설정에서 마이크 비활성화

#### 해결방법

**Windows:**
```
1. 제어판 → 소리 → 녹음 탭
2. 마이크 장치 활성화 확인
3. 기본 장치로 설정
```

**macOS:**
```
1. 시스템 환경설정 → 사운드 → 입력
2. 마이크 선택 및 입력 레벨 확인
3. Chrome/Safari에 마이크 권한 허용
   (시스템 환경설정 → 보안 및 개인정보 → 개인정보 → 마이크)
```

**Linux:**
```bash
# PulseAudio 마이크 확인
pactl list sources short

# ALSA 마이크 확인
arecord -l
```

---

### 문제 3: "이 브라우저는 음성인식을 지원하지 않습니다"

#### 증상
```
"이 브라우저는 음성인식을 지원하지 않습니다. Chrome 또는 Edge를 사용해주세요."
```

#### 원인
- Firefox, Safari에서 Web Speech API 사용 시도
- `window.SpeechRecognition` / `window.webkitSpeechRecognition` 미지원

#### 해결방법

**방법 1: Chrome/Edge 사용 (권장)**
```
Chrome 또는 Edge 브라우저로 전환
```

**방법 2: Local STT Model 사용**
```javascript
// useVoiceRecognition 대신 useVoiceRecognitionLocal 사용

// 변경 전
import { useVoiceRecognition } from '@/hooks/useVoiceRecognition';

// 변경 후
import { useVoiceRecognitionLocal } from '@/hooks/useVoiceRecognitionLocal';
```

**코드 수정 예시:**
```typescript
// Firefox/Safari에서 자동으로 Local STT 사용
const isChromium = 'chrome' in window;

const voiceHook = isChromium
  ? useVoiceRecognition({ onResult: handleVoiceResult })
  : useVoiceRecognitionLocal({ onResult: handleVoiceResult });
```

---

### 문제 4: "음성이 감지되지 않았습니다"

#### 증상
```
Error: no-speech
또는
"음성이 감지되지 않았습니다. 다시 시도해주세요."
```

#### 원인
- 마이크 볼륨이 너무 낮음
- 주변 소음이 심함
- 마이크가 음소거됨

#### 해결방법

**마이크 볼륨 확인:**
```
Windows: 제어판 → 소리 → 녹음 → 마이크 → 속성 → 수준 (80% 이상)
macOS: 시스템 환경설정 → 사운드 → 입력 → 입력 볼륨 (중간 이상)
```

**마이크 테스트:**
```javascript
// 개발자 도구 Console에서 실행
navigator.mediaDevices.getUserMedia({ audio: true })
  .then(stream => {
    const audioContext = new AudioContext();
    const analyser = audioContext.createAnalyser();
    const microphone = audioContext.createMediaStreamSource(stream);
    microphone.connect(analyser);

    const dataArray = new Uint8Array(analyser.frequencyBinCount);

    function checkVolume() {
      analyser.getByteFrequencyData(dataArray);
      const volume = dataArray.reduce((a, b) => a + b) / dataArray.length;
      console.log('Microphone volume:', volume);

      if (volume > 0) {
        console.log('✅ Microphone is working!');
      } else {
        console.log('❌ No sound detected');
      }
    }

    setInterval(checkVolume, 1000);
  })
  .catch(err => console.error('Microphone error:', err));
```

---

### 문제 5: "네트워크 오류가 발생했습니다" (Web Speech API)

#### 증상
```
Error: network
또는
"네트워크 오류가 발생했습니다."
```

#### 원인
- 인터넷 연결 끊김
- Google 음성인식 서버 접속 불가
- 방화벽/프록시 차단

#### 해결방법

**인터넷 연결 확인:**
```bash
# Google 음성인식 서버 접속 테스트
ping www.google.com
```

**방화벽 예외 추가:**
```
Chrome → 설정 → 개인정보 및 보안 → 보안 → 고급
→ "안전하지 않은 콘텐츠" 차단 해제
```

**대안: Local STT 사용**
```
인터넷 불안정 시 useVoiceRecognitionLocal로 전환
(서버 STT는 로컬 네트워크만 필요)
```

---

### 문제 6: HTTPS 필수 에러

#### 증상
```
Error: getUserMedia() requires HTTPS
또는
"보안 연결(HTTPS)이 필요합니다"
```

#### 원인
- HTTP에서 마이크 접근 시도 (보안 정책 위반)
- `localhost` 제외 HTTP 도메인

#### 해결방법

**개발 환경 (localhost):**
```bash
# localhost는 HTTPS 없이도 허용됨
npm run dev
# http://localhost:3000 (OK)
```

**프로덕션/배포 환경:**
```nginx
# Nginx HTTPS 설정 예시
server {
    listen 443 ssl;
    server_name triplan.example.com;

    ssl_certificate /etc/letsencrypt/live/triplan.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/triplan.example.com/privkey.pem;

    location / {
        proxy_pass http://localhost:3000;
    }
}
```

**Let's Encrypt 무료 SSL 인증서:**
```bash
# Certbot 설치 (Ubuntu/Debian)
sudo apt-get install certbot python3-certbot-nginx

# SSL 인증서 발급
sudo certbot --nginx -d triplan.example.com

# 자동 갱신 설정
sudo certbot renew --dry-run
```

---

## 에러 코드별 해결방법

### Web Speech API 에러 코드

코드 위치: `frontend/src/hooks/useVoiceRecognition.ts:116-129`

| 에러 코드 | 의미 | 사용자 메시지 | 해결방법 |
|----------|------|---------------|----------|
| `no-speech` | 음성 미감지 | "음성이 감지되지 않았습니다. 다시 시도해주세요." | 마이크 볼륨 확인, 주변 소음 감소 |
| `audio-capture` | 마이크 없음 | "마이크를 찾을 수 없습니다. 마이크를 연결해주세요." | 마이크 연결, OS 설정 확인 |
| `not-allowed` | 권한 거부 | "마이크 권한이 거부되었습니다. 브라우저 설정에서 마이크 권한을 허용해주세요." | 브라우저 권한 설정 변경 |
| `network` | 네트워크 오류 | "네트워크 오류가 발생했습니다." | 인터넷 연결 확인 |
| `aborted` | 사용자 중단 | (무시) | 정상 동작 |
| `service-not-allowed` | API 차단 | "음성인식 서비스를 사용할 수 없습니다." | Chrome/Edge 사용, HTTPS 확인 |

---

### MediaRecorder (Local STT) 에러 코드

코드 위치: `frontend/src/hooks/useAudioRecorder.ts:95-99`

| 에러 이름 | 의미 | 사용자 메시지 | 해결방법 |
|----------|------|---------------|----------|
| `NotAllowedError` | 권한 거부 | "마이크 권한이 거부되었습니다. 브라우저 설정에서 마이크 권한을 허용해주세요." | 브라우저 권한 설정 |
| `NotFoundError` | 마이크 없음 | "마이크를 찾을 수 없습니다. 마이크를 연결해주세요." | 마이크 연결, OS 설정 |
| `NotReadableError` | 하드웨어 충돌 | "마이크를 사용할 수 없습니다. 다른 앱에서 사용 중인지 확인하세요." | 다른 앱 종료 (Zoom, Discord 등) |
| `OverconstrainedError` | 설정 불가 | "마이크 설정이 지원되지 않습니다." | 코드의 audio constraints 변경 |
| `SecurityError` | HTTPS 필요 | "보안 연결(HTTPS)이 필요합니다." | HTTPS 사용 또는 localhost |

---

## HTTPS 요구사항

### 왜 HTTPS가 필요한가?

**보안 정책:**
- `getUserMedia()` API는 **Secure Context**에서만 동작
- HTTP에서는 **localhost/127.0.0.1만 예외** 허용
- 실제 도메인은 **HTTPS 필수**

```javascript
// ✅ 허용됨
http://localhost:3000
http://127.0.0.1:3000
https://triplan.example.com

// ❌ 차단됨
http://triplan.example.com
http://192.168.0.100:3000
```

---

### 개발 환경에서 HTTPS 사용하기

#### Next.js (권장)

```bash
# package.json에 스크립트 추가
{
  "scripts": {
    "dev": "next dev",
    "dev:https": "next dev --experimental-https"
  }
}

# HTTPS로 실행
npm run dev:https
# → https://localhost:3000
```

#### mkcert로 로컬 SSL 인증서 생성

```bash
# mkcert 설치 (macOS)
brew install mkcert
brew install nss # Firefox 지원

# 로컬 CA 설치
mkcert -install

# localhost 인증서 생성
mkcert localhost 127.0.0.1 ::1

# 생성된 파일
# localhost+2.pem (인증서)
# localhost+2-key.pem (비밀키)
```

**Next.js에 적용:**
```javascript
// server.js
const https = require('https');
const fs = require('fs');
const next = require('next');

const dev = process.env.NODE_ENV !== 'production';
const app = next({ dev });
const handle = app.getRequestHandler();

const httpsOptions = {
  key: fs.readFileSync('./localhost+2-key.pem'),
  cert: fs.readFileSync('./localhost+2.pem'),
};

app.prepare().then(() => {
  https.createServer(httpsOptions, (req, res) => {
    handle(req, res);
  }).listen(3000, (err) => {
    if (err) throw err;
    console.log('> Ready on https://localhost:3000');
  });
});
```

---

### 프로덕션 환경 HTTPS 설정

#### Nginx + Let's Encrypt (권장)

```nginx
# /etc/nginx/sites-available/triplan
server {
    listen 80;
    server_name triplan.example.com;

    # HTTP → HTTPS 리다이렉트
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name triplan.example.com;

    # SSL 인증서 (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/triplan.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/triplan.example.com/privkey.pem;

    # SSL 보안 설정
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Next.js 프록시
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Let's Encrypt 자동 갱신:**
```bash
# Certbot 설치
sudo apt-get install certbot python3-certbot-nginx

# 인증서 발급 및 Nginx 자동 설정
sudo certbot --nginx -d triplan.example.com

# 자동 갱신 테스트
sudo certbot renew --dry-run

# Cron 자동 갱신 (매일 2번 체크)
0 0,12 * * * certbot renew --quiet
```

---

## 디버깅 방법

### 1. 브라우저 Console 로그 확인

```javascript
// useVoiceRecognition.ts에서 로그 출력
console.log('🎤 Voice recognition started');   // 라인 70
console.log('🎤 Voice recognition ended');     // 라인 76
console.log('🎤 Final transcript:', text);     // 라인 97
console.log('🎤 Interim transcript:', text);   // 라인 105
console.error('🎤 Voice recognition error:', event.error);  // 라인 112
```

**확인 방법:**
```
F12 → Console 탭 → 필터: "🎤"
```

---

### 2. 마이크 권한 상태 확인

```javascript
// 개발자 도구 Console에서 실행
navigator.permissions.query({ name: 'microphone' })
  .then(result => {
    console.log('Permission state:', result.state);

    result.onchange = () => {
      console.log('Permission changed to:', result.state);
    };
  });

// 결과:
// "granted" = 허용됨 ✅
// "denied" = 거부됨 ❌
// "prompt" = 아직 묻지 않음 ⏳
```

---

### 3. 마이크 장치 목록 확인

```javascript
// 사용 가능한 마이크 목록
navigator.mediaDevices.enumerateDevices()
  .then(devices => {
    const microphones = devices.filter(d => d.kind === 'audioinput');
    console.log('Available microphones:', microphones);

    microphones.forEach((mic, i) => {
      console.log(`${i + 1}. ${mic.label || 'Microphone ' + (i + 1)}`);
      console.log('   Device ID:', mic.deviceId);
    });
  });

// 결과 예시:
// 1. MacBook Pro Microphone
//    Device ID: default
// 2. USB Microphone
//    Device ID: abc123...
```

---

### 4. 실시간 오디오 레벨 확인

```javascript
// 마이크 볼륨 시각화
navigator.mediaDevices.getUserMedia({ audio: true })
  .then(stream => {
    const audioContext = new AudioContext();
    const analyser = audioContext.createAnalyser();
    const microphone = audioContext.createMediaStreamSource(stream);
    const dataArray = new Uint8Array(analyser.frequencyBinCount);

    microphone.connect(analyser);
    analyser.fftSize = 256;

    function visualize() {
      analyser.getByteFrequencyData(dataArray);
      const average = dataArray.reduce((a, b) => a + b) / dataArray.length;

      const bar = '█'.repeat(Math.floor(average / 5));
      console.log(`Volume: ${bar} ${average.toFixed(0)}`);

      requestAnimationFrame(visualize);
    }

    visualize();
  })
  .catch(err => console.error('Error:', err));

// 결과 예시:
// Volume: ████████ 40
// Volume: ████████████ 60
// Volume: ██████ 30
```

---

### 5. Backend STT API 디버깅 (Local STT)

#### Frontend 로그

```javascript
// useVoiceRecognitionLocal.ts
console.log('🎤 Audio recording complete, sending to STT API...');  // 라인 33
console.log('✅ Transcription:', data.text);  // 라인 68
console.error('❌ Transcription error:', error);  // 라인 79
```

#### Backend API 테스트

```bash
# 마이크 녹음 테스트 (임시 파일 생성)
# Chrome DevTools → Network → stt/transcribe → Request → Copy as cURL

# 또는 직접 테스트
TOKEN="your-jwt-token-here"

curl -X POST http://localhost:8000/api/chat/stt/transcribe/ \
  -H "Authorization: Bearer $TOKEN" \
  -F "audio=@recording.webm"

# 응답 예시:
# {
#   "success": true,
#   "text": "제주도 맛집 추천해줘",
#   "confidence": 0.95
# }
```

#### Backend 로그 확인

```bash
# Docker 로그
docker-compose logs -f backend | grep "STT"

# Django 로그
# backend/apps/chat/views.py에서 로그 확인
```

---

### 6. Web Speech API 지원 확인

```javascript
// SpeechRecognition 지원 여부
function checkSpeechRecognitionSupport() {
  const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;

  if (!SpeechRecognition) {
    console.error('❌ SpeechRecognition not supported');
    console.log('Browser:', navigator.userAgent);
    return false;
  }

  console.log('✅ SpeechRecognition supported');

  const recognition = new SpeechRecognition();
  console.log('Supported properties:', {
    continuous: 'continuous' in recognition,
    interimResults: 'interimResults' in recognition,
    lang: 'lang' in recognition,
  });

  return true;
}

checkSpeechRecognitionSupport();
```

---

## 사용자 가이드

### 일반 사용자를 위한 간단 가이드

#### 1단계: 브라우저 확인

```
✅ 권장 브라우저:
- Google Chrome (최신 버전)
- Microsoft Edge (최신 버전)

⚠️ 제한적 지원:
- Firefox (Local STT만 가능)
- Safari (Local STT만 가능)
```

---

#### 2단계: 마이크 권한 허용

**처음 사용 시:**
```
1. 🎤 마이크 버튼 클릭
2. 브라우저 상단에 "마이크 사용 허용" 팝업 표시
3. "허용" 버튼 클릭
4. 완료!
```

**권한이 거부된 경우:**
```
Chrome:
1. 주소창 왼쪽 자물쇠 🔒 클릭
2. "사이트 설정" 클릭
3. 마이크 → "허용" 선택
4. 페이지 새로고침 (F5)

Firefox:
1. 주소창 왼쪽 자물쇠 🔒 클릭
2. 권한 → 마이크 사용 → 허용
3. 페이지 새로고침 (F5)
```

---

#### 3단계: 마이크 테스트

**음성인식이 작동하지 않을 때:**

1. **마이크 연결 확인**
   - 마이크가 컴퓨터에 올바르게 연결되어 있나요?
   - 무선 마이크의 경우 배터리가 충전되어 있나요?

2. **볼륨 확인**
   - Windows: 우하단 🔊 → 마이크 → 볼륨 80% 이상
   - Mac: 시스템 환경설정 → 사운드 → 입력 → 볼륨 중간 이상

3. **다른 앱 종료**
   - Zoom, Discord, Skype 등 마이크 사용 중인 앱 종료

4. **브라우저 재시작**
   - Chrome/Edge를 완전히 종료 후 재실행

---

#### 4단계: 음성 입력 사용

**Web Speech API (실시간):**
```
1. 🎤 버튼 클릭 (빨간색으로 변함)
2. "제주도 맛집 추천해줘" 말하기
3. 실시간으로 텍스트가 입력됨
4. 말이 끝나면 자동으로 인식 완료
5. ✈️ 전송 버튼 클릭
```

**Local STT (녹음 후 전송):**
```
1. 🎤 버튼 클릭 (녹음 시작)
2. "제주도 맛집 추천해줘" 말하기
3. 🎤 버튼 다시 클릭 (녹음 종료)
4. 서버로 전송 중... (로딩)
5. 텍스트로 변환 완료
6. ✈️ 전송 버튼 클릭
```

---

### 관리자를 위한 트러블슈팅 체크리스트

#### 🔍 1차 진단 (사용자에게 확인 요청)

- [ ] Chrome 또는 Edge 브라우저를 사용 중인가?
- [ ] 브라우저가 최신 버전인가? (chrome://settings/help)
- [ ] 마이크 권한을 허용했는가? (주소창 자물쇠 확인)
- [ ] 마이크가 컴퓨터에 연결되어 있는가?
- [ ] 다른 앱(Zoom, Discord 등)에서 마이크를 사용 중인가?
- [ ] HTTP가 아닌 HTTPS를 사용 중인가? (또는 localhost)

#### 🔧 2차 진단 (개발자 도구)

- [ ] Console에 에러 메시지가 있는가? (F12 → Console)
- [ ] Network 탭에서 API 요청이 실패하는가? (F12 → Network)
- [ ] `navigator.mediaDevices.getUserMedia` 호출 시 에러는?
- [ ] `navigator.permissions.query({ name: 'microphone' })` 결과는?

#### 🚀 3차 진단 (서버/인프라)

- [ ] Backend STT API가 정상 동작하는가? (`/api/chat/stt/health/`)
- [ ] STT 모델 파일이 존재하는가? (`model.safetensors`)
- [ ] Nginx HTTPS 설정이 올바른가?
- [ ] SSL 인증서가 유효한가?
- [ ] CORS 설정이 올바른가?

---

## 코드 참조

### 관련 파일

| 파일 | 설명 |
|------|------|
| `frontend/src/hooks/useVoiceRecognition.ts` | Web Speech API 기반 실시간 음성인식 |
| `frontend/src/hooks/useVoiceRecognitionLocal.ts` | Local STT 모델 기반 음성인식 |
| `frontend/src/hooks/useAudioRecorder.ts` | MediaRecorder 기반 오디오 녹음 |
| `frontend/src/components/planner/UnifiedChatWidget.tsx` | 음성인식 UI 통합 |
| `backend/apps/chat/views.py` | STT API 엔드포인트 |
| `nginx/nginx.conf` | HTTPS 설정 |

---

### 주요 코드 스니펫

#### 브라우저별 음성인식 자동 선택

```typescript
import { useVoiceRecognition } from '@/hooks/useVoiceRecognition';
import { useVoiceRecognitionLocal } from '@/hooks/useVoiceRecognitionLocal';

function ChatWidget() {
  // Chrome/Edge → Web Speech API
  // Firefox/Safari → Local STT
  const isChromiumBased = 'chrome' in window || navigator.userAgent.includes('Edg');

  const webSpeechHook = useVoiceRecognition({
    onResult: handleVoiceResult,
    onError: handleVoiceError,
    lang: 'ko-KR',
  });

  const localSTTHook = useVoiceRecognitionLocal({
    onResult: handleVoiceResult,
    onError: handleVoiceError,
  });

  const voiceHook = webSpeechHook.isSupported ? webSpeechHook : localSTTHook;

  return (
    <button onClick={voiceHook.startListening}>
      {voiceHook.isListening ? '🔴 녹음 중...' : '🎤 음성 입력'}
    </button>
  );
}
```

---

#### 에러 처리 개선

```typescript
function handleVoiceError(error: string) {
  console.error('Voice error:', error);

  // 사용자 친화적 메시지
  let userMessage = error;
  let actionButton = null;

  if (error.includes('권한이 거부')) {
    userMessage = '마이크 권한이 필요합니다.';
    actionButton = {
      label: '권한 설정 방법 보기',
      action: () => window.open('/help/microphone-permission', '_blank'),
    };
  } else if (error.includes('지원하지 않습니다')) {
    userMessage = 'Chrome 또는 Edge 브라우저를 사용해주세요.';
    actionButton = {
      label: 'Chrome 다운로드',
      action: () => window.open('https://www.google.com/chrome/', '_blank'),
    };
  }

  // Toast 알림 표시
  showToast({
    type: 'error',
    message: userMessage,
    action: actionButton,
    duration: 5000,
  });
}
```

---

## 추가 자료

### 공식 문서

- **Web Speech API**: https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API
- **MediaStream API**: https://developer.mozilla.org/en-US/docs/Web/API/MediaStream_API
- **getUserMedia()**: https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia
- **Permissions API**: https://developer.mozilla.org/en-US/docs/Web/API/Permissions_API

### 브라우저 지원 현황

- **Can I Use - Web Speech API**: https://caniuse.com/speech-recognition
- **Can I Use - MediaRecorder**: https://caniuse.com/mediarecorder

### HTTPS 설정

- **Let's Encrypt**: https://letsencrypt.org/
- **Certbot**: https://certbot.eff.org/
- **mkcert (로컬 SSL)**: https://github.com/FiloSottile/mkcert

---

## 요약

### 빠른 해결 체크리스트

**사용자:**
1. ✅ Chrome/Edge 사용
2. ✅ 마이크 권한 허용 (주소창 자물쇠)
3. ✅ 마이크 연결 및 볼륨 확인
4. ✅ 다른 앱에서 마이크 사용 중단
5. ✅ HTTPS 또는 localhost 사용

**개발자:**
1. ✅ 브라우저별 Fallback 구현 (Web Speech ↔ Local STT)
2. ✅ 명확한 에러 메시지 제공
3. ✅ 권한 요청 전 안내 문구 표시
4. ✅ HTTPS 강제 리다이렉트 설정
5. ✅ Backend STT API 상태 모니터링

---

**생성일:** 2025-01-18
**작성자:** Claude Code
**버전:** 1.0
