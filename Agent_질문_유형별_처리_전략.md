# Agent 질문 유형별 처리 전략

## 목차
1. [문제 정의](#문제-정의)
2. [질문 유형 분류](#질문-유형-분류)
3. [현재 시스템 분석](#현재-시스템-분석)
4. [개선 전략](#개선-전략)
5. [프롬프트 개선안](#프롬프트-개선안)
6. [도구 추가 제안](#도구-추가-제안)
7. [실제 동작 예시](#실제-동작-예시)

---

## 문제 정의

### 사용자 질문의 다양성

사용자는 크게 **2가지 유형**의 질문을 합니다:

| 질문 유형 | 예시 | 현재 처리 방식 | 문제점 |
|----------|------|--------------|--------|
| **1. 여행 코스 추천** | "제주도 2박 3일 추천해줘" | ✅ `recommend_similar_trips` (RAG) | 잘 작동 |
| **2. 일반 정보 질문** | "여수에 맛있는게 뭐가있어?" | ⚠️ GPT-4 지식 또는 `recommend_places` | 일관성 없음 |

### 핵심 이슈

```
문제: GPT-4가 "일반 질문"과 "여행 코스 추천"을 구분하지 못함

사용자: "여수에 맛있는게 뭐가있어?"

현재 Agent 행동 (불일치):
  케이스 1: GPT-4 자체 지식으로 답변 (2023년 데이터)
           → "여수 게장, 서대회, 갓김치가 유명합니다"

  케이스 2: recommend_places 도구 사용 (DB 데이터)
           → DB에 여수 맛집 5개 반환

  케이스 3: recommend_similar_trips 도구 사용 (RAG 데이터)
           → "여수 맛집 투어" 영상 검색

→ 사용자 입장에서 답변이 매번 달라짐!
```

---

## 질문 유형 분류

### 1️⃣ 여행 코스 추천 (RAG 필수)

**특징:**
- 구체적인 일정이 필요
- 여러 장소를 묶은 코스
- "추천", "일정", "코스" 키워드

**예시:**
```
✅ "제주도 2박 3일 추천해줘"
✅ "서울 1박 2일 맛집 투어 짜줘"
✅ "부산 여행 코스 알려줘"
✅ "경주 당일치기 일정"
```

**적절한 도구:**
- `recommend_similar_trips` (RAG) - **최우선**
- `recommend_places` - 보조

---

### 2️⃣ 장소 정보 질문 (DB 또는 GPT-4)

**특징:**
- 특정 지역의 정보
- 단일 질문 (일정 불필요)
- "뭐가 있어?", "유명한 곳", "추천" 키워드

**예시:**
```
✅ "여수에 맛있는게 뭐가있어?"
✅ "제주도 유명한 관광지는?"
✅ "강남 카페 추천해줘"
✅ "부산 해운대 근처 맛집"
```

**적절한 도구:**
- `recommend_places` (DB 평점 기반) - **최우선**
- GPT-4 지식 - 보조 (DB 없을 시)

---

### 3️⃣ 일반 대화 (GPT-4만)

**특징:**
- 여행과 무관한 질문
- 인사, 잡담, 확인

**예시:**
```
✅ "안녕하세요"
✅ "고마워"
✅ "날씨 어때?"
✅ "예산이 얼마나 들까?"
```

**적절한 도구:**
- 없음 (GPT-4 직접 응답)

---

### 4️⃣ 일정 조작 (도구 필수)

**특징:**
- 플래너 수정 요청
- "추가", "삭제", "변경" 키워드

**예시:**
```
✅ "Day 1에 성산일출봉 추가해줘"
✅ "Day 2 일정 삭제해줘"
✅ "여행 날짜 변경해줘"
```

**적절한 도구:**
- `add_place_to_day`, `delete_schedule`, `update_trip_dates` 등

---

## 현재 시스템 분석

### 도구 목록 (15개)

```python
tools = [
    # 📊 조회
    get_planner_info,

    # 🔍 검색 (5개)
    search_place,              # 특정 장소 검색 (DB + 카카오맵)
    get_place_details,         # 장소 상세 정보
    search_nearby,             # 주변 장소 검색
    recommend_places,          # 인기 장소 추천 (DB)
    search_and_show_on_map,    # 지도에서 검색

    # ✨ AI 추천
    recommend_similar_trips,   # 여행 코스 추천 (RAG)

    # 📍 일정 관리 (8개)
    add_place_to_day,
    update_schedule,
    move_schedule,
    reorder_schedule,
    delete_schedule,
    delete_all_schedules,
    update_trip_info,
    update_trip_dates,
]
```

---

### 현재 프롬프트 (v1.py)

```python
사용자 요청 처리 가이드:
- "어디갈지 추천해줘", "여행지 추천", "유명한 코스 알려줘" 같은 요청:
  1. 먼저 get_planner_info로 현재 여행 정보를 확인
  2. **recommend_similar_trips**를 사용하여 실제 여행 데이터 기반 추천 (RAG)
  3. 필요시 recommend_places로 인기 장소 추가 추천

- 구체적인 지역이 없으면 사용자에게 어떤 지역이나 카테고리를 원하는지 물어보세요.
```

**문제점:**
- ❌ "여수에 맛있는게 뭐가있어?" 같은 **장소 정보 질문**에 대한 가이드 없음
- ❌ GPT-4가 자체 판단으로 도구를 선택 → 일관성 부족

---

## 개선 전략

### 전략 1: 프롬프트에 질문 유형별 가이드 추가 ✅ (권장)

**장점:**
- ✅ 구현 쉬움 (프롬프트만 수정)
- ✅ GPT-4의 자연어 이해 능력 활용
- ✅ 유연한 처리 가능

**단점:**
- ⚠️ GPT-4 판단에 의존 (100% 정확도 보장 어려움)

---

### 전략 2: 사전 분류기 추가 (Intent Classifier) ⚠️

**구조:**
```
사용자 입력
    ↓
Intent Classifier (별도 모델 또는 규칙 기반)
    ↓
┌──────────────┬──────────────┬──────────────┐
│ 여행 코스    │ 장소 정보    │ 일정 조작    │
│ 추천         │ 질문         │              │
└──────────────┴──────────────┴──────────────┘
    ↓              ↓              ↓
RAG 우선       DB 우선       도구 사용
```

**장점:**
- ✅ 명확한 분류
- ✅ 일관된 처리

**단점:**
- ❌ 추가 개발 필요
- ❌ 규칙 유지보수 필요
- ❌ 복잡한 질문 처리 어려움

---

### 전략 3: 도구 설명 개선 ✅ (권장)

**현재 `recommend_places` 도구:**
```python
@tool
def recommend_places(region: str = None, min_rating: float = 4.0,
                     category: str = None, limit: int = 10) -> str:
    """인기 장소를 추천합니다.

    Args:
        region: 지역 필터 (선택, 예: '서울', '제주')
        min_rating: 최소 평점 (기본값: 4.0)
        category: 카테고리 필터 (선택, 예: '관광지', '맛집')
        limit: 결과 개수 (기본값: 10)

    Returns:
        추천 장소 목록 (JSON 형식)
    """
```

**개선안:**
```python
@tool
def recommend_places(region: str = None, min_rating: float = 4.0,
                     category: str = None, limit: int = 10) -> str:
    """인기 장소를 추천합니다.

    **사용 시점:**
    - 사용자가 특정 지역의 장소 정보를 물어볼 때 (예: "여수에 맛있는게 뭐가있어?")
    - 카테고리별 인기 장소를 추천할 때 (예: "제주도 맛집 추천해줘")
    - 여행 코스가 아닌 단일 질문에 답변할 때

    **주의:**
    - 여행 일정/코스 추천은 recommend_similar_trips를 사용하세요
    - 이 도구는 DB 평점 기반 인기 장소만 반환합니다

    Args:
        region: 지역 필터 (필수, 예: '여수', '제주', '서울')
        category: 카테고리 필터 (선택, 예: '맛집', '관광지', '카페')
        min_rating: 최소 평점 (기본값: 4.0)
        limit: 결과 개수 (기본값: 10)

    Returns:
        추천 장소 목록 (JSON 형식)
    """
```

**효과:**
- ✅ GPT-4가 도구 선택 시 명확한 기준 제공
- ✅ 사용 시점 명시로 혼동 방지

---

## 프롬프트 개선안

### v1.1 프롬프트 (수정안)

```python
# backend/apps/chat/prompts/v1.py

사용자 요청 처리 가이드:

1️⃣ 여행 코스/일정 추천 요청:
   예: "제주도 2박 3일 추천해줘", "서울 여행 코스 알려줘"

   처리 순서:
   1. get_planner_info로 현재 여행 정보 확인
   2. **recommend_similar_trips**를 사용하여 실제 여행 데이터 기반 추천 (RAG)
      → 실제 여행자들이 다녀온 코스를 제공
   3. 필요시 recommend_places로 인기 장소 추가 추천

2️⃣ 장소 정보 질문:
   예: "여수에 맛있는게 뭐가있어?", "제주도 유명한 관광지는?"

   처리 순서:
   1. **recommend_places**를 사용하여 DB 평점 기반 인기 장소 추천
      → region, category 파라미터를 적절히 설정
   2. DB에 데이터가 없으면 GPT-4 지식으로 보조 답변
   3. 사용자에게 "실제 여행 코스를 보고 싶으면 recommend_similar_trips를 사용하세요" 안내

3️⃣ 일반 대화:
   예: "안녕하세요", "고마워", "날씨 어때?"

   처리:
   - 도구 사용 없이 GPT-4로 직접 답변

4️⃣ 일정 조작:
   예: "Day 1에 성산일출봉 추가해줘"

   처리:
   - 적절한 일정 관리 도구 사용 (add_place_to_day 등)

**중요한 구분:**
- "추천해줘" + 일정/코스 언급 → recommend_similar_trips (RAG)
- "추천해줘" + 단일 지역/카테고리 → recommend_places (DB)
- "뭐가 있어?" → recommend_places (DB)
```

---

## 도구 추가 제안

### 새 도구: `answer_travel_question` (선택)

```python
@tool
def answer_travel_question(question: str, region: str = None) -> str:
    """여행 관련 일반 질문에 답변합니다.

    **사용 시점:**
    - 사용자가 특정 지역에 대한 정보를 물어볼 때
    - 여행 코스가 아닌 단순 정보 질문
    - 예: "여수에 맛있는게 뭐가있어?", "제주도 날씨는?"

    **주의:**
    - 여행 코스 추천은 recommend_similar_trips 사용
    - 구체적인 장소 추천은 recommend_places 사용

    이 도구는 GPT-4 지식을 활용하여 일반적인 답변을 제공합니다.

    Args:
        question: 질문 내용
        region: 관련 지역 (선택)

    Returns:
        답변 텍스트
    """
    from openai import chat

    # GPT-4로 답변 생성
    response = openai.chat.completions.create(
        model="gpt-4-turbo-preview",
        messages=[
            {"role": "system", "content": f"당신은 여행 전문가입니다. {region}에 대한 질문에 답변하세요."},
            {"role": "user", "content": question}
        ],
        temperature=0.7,
        max_tokens=500
    )

    answer = response.choices[0].message.content

    # DB 데이터 보조 제안
    footer = f"\n\n💡 {region}의 실제 평점 높은 장소를 보고 싶으시면 'recommend_places'를 사용해보세요!"

    return answer + footer
```

**장단점:**
| 장점 | 단점 |
|------|------|
| ✅ 질문 유형 명확히 분리 | ❌ GPT-4 API 비용 증가 (2배) |
| ✅ 일관된 답변 형식 | ❌ 응답 시간 증가 |
| ✅ DB 데이터와 GPT 지식 결합 | ❌ 도구 개수 증가 (복잡도 증가) |

**결론:** ⚠️ **비추천** - 프롬프트 개선으로 충분

---

## 실제 동작 예시

### 예시 1: "여수에 맛있는게 뭐가있어?"

#### ❌ 현재 (불일치)

```
사용자: "여수에 맛있는게 뭐가있어?"

케이스 A (GPT-4 직접 답변):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
여수는 해산물이 유명합니다!

1. **게장**: 여수 게장은 전국적으로 유명해요
2. **서대회**: 신선한 서대를 횟감으로
3. **갓김치**: 여수 돌산 갓김치
4. **돌산 갓김치**: ...

→ 2023년 GPT-4 지식 기반 (DB 데이터 없음)

케이스 B (recommend_places 도구):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
여수 인기 맛집을 찾았습니다!

1. **거북선 횟집** ⭐ 4.7
   - 주소: 여수시 돌산읍
   - 카테고리: 한식 > 해산물

2. **돌산갓김치마을** ⭐ 4.5
   ...

→ DB 평점 기반 실제 데이터

케이스 C (recommend_similar_trips 도구):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 '여수 맛집' 검색 결과:

1. **여수 1박 2일 맛집 투어** (유사도: 88%)
   📺 채널: 맛집유튜버
   🗓️ Day 1: 거북선 횟집, 돌산갓김치마을...
   🔗 [영상 보기](https://youtube.com/...)

→ RAG 여행 코스 데이터
```

#### ✅ 개선 후 (일관적)

```
사용자: "여수에 맛있는게 뭐가있어?"

Agent 판단:
  → "장소 정보 질문" 유형으로 분류
  → recommend_places 도구 사용 (프롬프트 가이드 따름)

Agent 응답:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
여수 인기 맛집을 찾았습니다!

1. **거북선 횟집** ⭐ 4.7 (리뷰 1,234개)
   - 주소: 여수시 돌산읍 돌산로 123
   - 카테고리: 한식 > 해산물
   - 특징: 신선한 활어, 바다 전망

2. **돌산갓김치마을** ⭐ 4.5 (리뷰 890개)
   - 주소: 여수시 돌산읍 갓밭길 45
   - 카테고리: 한식 > 반찬
   - 특징: 전통 갓김치, 게장 세트

3. **서대회센터** ⭐ 4.6 (리뷰 1,100개)
   - 주소: 여수시 수정동 해안로 67
   - 카테고리: 한식 > 횟집
   - 특징: 서대회 전문, 제철 해산물

💡 **Tip:** 여수 맛집 투어 일정을 짜고 싶으시면
"여수 1박 2일 맛집 투어 추천해줘"라고 물어보세요!

→ 항상 recommend_places 사용 (일관성 ✅)
```

---

### 예시 2: "제주도 2박 3일 추천해줘"

#### ✅ 현재 (이미 잘 작동)

```
사용자: "제주도 2박 3일 추천해줘"

Agent 판단:
  → "여행 코스 추천" 유형으로 분류
  → recommend_similar_trips 도구 사용 (프롬프트 가이드 따름)

Agent 응답:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 '제주도 2박 3일 자연 여행' 검색 결과 (5개의 추천):

1. **제주 동부 일주 완벽 코스** (유사도: 92%)
   📺 채널: 여행유튜버
   🗓️ Day 1: 성산일출봉, 섭지코지, 광치기해변
      Day 2: 만장굴, 비자림, 함덕해수욕장
      Day 3: 우도, 성산포구, 흑돼지 맛집
   🔗 [영상 보기](https://youtube.com/...)

2. **제주 서부 맛집 투어** (유사도: 88%)
   ...

어떤 코스를 참고하시겠어요?

→ recommend_similar_trips (RAG) 사용 ✅
```

---

### 예시 3: "여수 맛집 투어 일정 짜줘"

#### ❌ 현재 (모호함)

```
사용자: "여수 맛집 투어 일정 짜줘"

Agent 판단 (모호):
  → "맛집" 키워드 → recommend_places?
  → "일정 짜줘" 키워드 → recommend_similar_trips?

→ GPT-4 판단에 따라 다름 (불일치)
```

#### ✅ 개선 후

```
사용자: "여수 맛집 투어 일정 짜줘"

Agent 판단 (프롬프트 가이드):
  → "일정 짜줘" 키워드 → "여행 코스 추천" 유형
  → recommend_similar_trips 도구 사용

Agent 응답:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 '여수 맛집 투어' 검색 결과:

1. **여수 1박 2일 맛집 완전정복** (유사도: 91%)
   📺 채널: 맛집유튜버
   🗓️ Day 1: 거북선 횟집(점심) → 돌산갓김치마을(저녁)
      Day 2: 서대회센터(점심) → 여수밤바다(야경)
   💰 예산: 1인 15만원
   🔗 [영상 보기](https://youtube.com/...)

2. **여수 당일치기 해산물 투어** (유사도: 85%)
   ...

→ recommend_similar_trips (RAG) 우선 사용 ✅
```

---

## 구현 계획

### Phase 1: 프롬프트 개선 (즉시 적용 가능)

```python
# backend/apps/chat/prompts/v1.py 수정

def get_system_prompt(trip_id: int, room_id: int) -> str:
    return f"""당신은 전문 여행 플래너 비서입니다.

당신의 역할:
... (기존과 동일) ...

사용 가능한 도구:
... (기존과 동일) ...

🆕 사용자 요청 처리 가이드 (개선):

1️⃣ 여행 코스/일정 추천 요청:
   키워드: "일정", "코스", "추천해줘" + "N박M일"
   예시: "제주도 2박 3일 추천해줘", "여수 맛집 투어 일정 짜줘"

   처리:
   1. get_planner_info로 현재 여행 정보 확인
   2. **recommend_similar_trips** 사용 (RAG, 최우선)
   3. 보조: recommend_places

2️⃣ 장소 정보 질문:
   키워드: "뭐가 있어?", "유명한 곳", "추천" (일정 언급 없음)
   예시: "여수에 맛있는게 뭐가있어?", "제주도 관광지는?"

   처리:
   1. **recommend_places** 사용 (DB 평점, 최우선)
      → region, category 파라미터 적절히 설정
   2. DB 없으면: GPT-4 지식으로 보조 답변
   3. 안내: "실제 여행 코스를 보려면 '~일정 짜줘'라고 물어보세요"

3️⃣ 일반 대화:
   예시: "안녕하세요", "고마워"

   처리: 도구 없이 직접 답변

4️⃣ 일정 조작:
   예시: "Day 1에 추가해줘"

   처리: 일정 관리 도구 사용

**핵심 구분:**
- "일정/코스 + 추천" → recommend_similar_trips (RAG)
- "지역 + 장소/맛집 + 뭐가있어" → recommend_places (DB)
"""
```

---

### Phase 2: 도구 설명 개선 (즉시 적용 가능)

```python
# backend/apps/chat/agent.py 수정

@tool
def recommend_places(region: str = None, min_rating: float = 4.0,
                     category: str = None, limit: int = 10) -> str:
    """🆕 인기 장소를 추천합니다 (DB 평점 기반).

    **사용 시점:**
    - 사용자가 특정 지역의 장소를 물어볼 때
      예: "여수에 맛있는게 뭐가있어?", "제주도 관광지는?"
    - 카테고리별 인기 장소 추천
      예: "강남 카페 추천해줘", "부산 맛집 알려줘"

    **주의:**
    - 여행 일정/코스 추천은 recommend_similar_trips 사용!
    - 이 도구는 DB 평점 기반 인기 장소만 반환합니다

    Args:
        region: 지역 필터 (필수, 예: '여수', '제주', '서울')
        category: 카테고리 (선택, 예: '맛집', '관광지', '카페')
        min_rating: 최소 평점 (기본값: 4.0)
        limit: 결과 개수 (기본값: 10)

    Returns:
        추천 장소 목록 (JSON 형식)
    """
    # ... 기존 코드 ...


@tool
def recommend_similar_trips(query: str, limit: int = 5) -> str:
    """🆕 실제 여행 블로그/영상 데이터를 기반으로 여행 경로를 추천합니다 (RAG).

    **사용 시점:**
    - 사용자가 여행 일정/코스를 요청할 때
      예: "제주도 2박 3일 추천해줘", "여수 맛집 투어 일정 짜줘"
    - "어디갈지 추천", "유명한 코스 알려줘" 같은 요청

    **주의:**
    - 단순 장소 정보 질문은 recommend_places 사용!
      예: "여수에 맛있는게 뭐가있어?" → recommend_places
    - 이 도구는 실제 여행자들의 코스 데이터를 검색합니다

    Args:
        query: 검색 쿼리 (예: "서울 3박4일", "제주도 맛집 투어")
        limit: 추천할 여행 경로 개수 (기본값: 5)

    Returns:
        추천 여행 경로 목록 (제목, URL, 일정, 유사도)
    """
    # ... 기존 코드 ...
```

---

### Phase 3: 응답 형식 통일 (선택)

```python
# recommend_places 응답에 Tip 추가

def recommend_places(...):
    # ... 기존 로직 ...

    response_lines = [f"🍴 {region} 인기 {category or '장소'}를 찾았습니다!"]

    for place in places:
        response_lines.append(f"{idx}. **{place.name}** ⭐ {place.rating}")
        response_lines.append(f"   - 주소: {place.address}")
        response_lines.append(f"   - 카테고리: {place.category}")

    # 🆕 Tip 추가
    response_lines.append(f"\n💡 **Tip:** {region} 여행 일정을 짜고 싶으시면")
    response_lines.append(f"'{region} N박M일 추천해줘'라고 물어보세요!")

    return "\n".join(response_lines)
```

---

## 요약

### 현재 문제점

| 문제 | 원인 |
|------|------|
| ❌ "여수에 맛있는게 뭐가있어?" 같은 질문에 답변이 매번 다름 | 프롬프트에 가이드 없음 |
| ❌ GPT-4가 `recommend_places`와 `recommend_similar_trips` 혼동 | 도구 설명 모호함 |
| ❌ DB 데이터와 GPT-4 지식이 섞여 혼란 | 우선순위 불명확 |

### 해결 방안

| 방법 | 난이도 | 효과 | 권장 |
|------|--------|------|------|
| **프롬프트 개선** | ⭐ 쉬움 | ⭐⭐⭐ 높음 | ✅ **강력 추천** |
| **도구 설명 개선** | ⭐ 쉬움 | ⭐⭐ 중간 | ✅ 권장 |
| **응답 형식 통일** | ⭐⭐ 보통 | ⭐⭐ 중간 | ✅ 권장 |
| 사전 분류기 추가 | ⭐⭐⭐ 어려움 | ⭐⭐⭐ 높음 | ⚠️ 과도함 |
| 새 도구 추가 | ⭐⭐ 보통 | ⭐ 낮음 | ❌ 비추천 |

### 권장 조치

1. ✅ **즉시**: 프롬프트 v1.1 적용 (질문 유형별 가이드 추가)
2. ✅ **즉시**: 도구 설명 개선 (사용 시점 명시)
3. ✅ **선택**: 응답 형식에 Tip 추가 (사용자 가이드)

**예상 효과:**
- 90% 이상 일관된 답변
- 사용자 만족도 향상
- RAG 활용도 증가

---

**생성일:** 2025-01-18
**작성자:** Claude Code
**버전:** 1.0
