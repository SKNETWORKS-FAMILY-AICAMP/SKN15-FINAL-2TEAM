# AI 모델 설정 및 파라미터 문서

## 개요
이 문서는 Triplan 프로젝트에서 사용하는 모든 AI 모델과 파라미터 설정을 설명합니다.
ChatRequest 저장 시 어떤 모델과 설정값으로 요청이 처리되었는지 추적할 수 있습니다.

---

## 1. Agent LLM (채팅 봇 주 모델)

### 모델 정보

| 항목 | 값 | 설정 위치 |
|------|-----|-----------|
| **모델명** | `gpt-4-turbo-preview` | `backend/config/settings/base.py:179` |
| **Temperature** | `0.7` | `backend/config/settings/base.py:180` |
| **API Key** | 환경변수 `OPENAI_API_KEY` | `backend/config/settings/base.py:175` |
| **사용 목적** | 여행 플래너 비서 Agent (도구 선택, 대화 처리) | `backend/apps/chat/agent.py:29-33` |

### 코드 구현

```python
# backend/apps/chat/agent.py (라인 28-33)

# LLM 초기화 (환경변수에서 모델과 temperature 설정)
self.llm = ChatOpenAI(
    model=getattr(settings, 'CHAT_AGENT_MODEL', 'gpt-4-turbo-preview'),
    temperature=float(getattr(settings, 'CHAT_AGENT_TEMPERATURE', 0.7)),
    openai_api_key=settings.OPENAI_API_KEY
)
```

### 설정 파일

```python
# backend/config/settings/base.py (라인 175-180)

OPENAI_API_KEY = env('OPENAI_API_KEY', default='')

# Chat Agent 설정
CHAT_AGENT_MODEL = env('CHAT_AGENT_MODEL', default='gpt-4-turbo-preview')
CHAT_AGENT_TEMPERATURE = env('CHAT_AGENT_TEMPERATURE', default='0.7')
```

### 환경변수 (.env)

```bash
# OpenAI API
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxx

# Agent 모델 설정 (선택, 기본값 사용 시 생략 가능)
CHAT_AGENT_MODEL=gpt-4-turbo-preview
CHAT_AGENT_TEMPERATURE=0.7
```

---

## 2. RAG Embedding Model (벡터 검색용)

### 모델 정보

| 항목 | 값 | 설정 위치 |
|------|-----|-----------|
| **모델명** | `text-embedding-ada-002` | `backend/apps/ai/rag.py:40` |
| **벡터 차원** | `1536` | OpenAI 고정값 |
| **API Key** | 환경변수 `OPENAI_API_KEY` | `backend/apps/ai/rag.py:26` |
| **사용 목적** | 여행 코스 텍스트 → 벡터 변환 (RAG 검색) | `backend/apps/ai/rag.py:28-48` |

### 코드 구현

```python
# backend/apps/ai/rag.py (라인 24-48)

class TripRAG:
    """여행 경로 RAG 시스템"""

    def __init__(self):
        """OpenAI 클라이언트 초기화"""
        openai.api_key = settings.OPENAI_API_KEY

    def create_query_embedding(self, query: str) -> List[float]:
        """
        쿼리 텍스트를 벡터 임베딩으로 변환

        Returns:
            1536차원 임베딩 벡터
        """
        response = openai.embeddings.create(
            model="text-embedding-ada-002",  # ← 임베딩 모델
            input=query
        )
        embedding = response.data[0].embedding
        return embedding  # 1536-dimensional vector
```

### 특징

- **모델 타입**: Embedding (텍스트 → 벡터)
- **Temperature 없음**: Embedding 모델은 deterministic (결정론적)
- **출력**: 1536차원 float32 배열 (6,144 bytes)
- **비용**: GPT-4보다 저렴 (약 1/100 수준)

---

## 3. Fallback GPT-4 (RAG 결과 없을 시)

### 모델 정보

| 항목 | 값 | 설정 위치 |
|------|-----|-----------|
| **모델명** | `gpt-4-turbo-preview` | `backend/apps/ai/rag.py:400` |
| **Temperature** | `0.7` | `backend/apps/ai/rag.py:405` |
| **Max Tokens** | `2000` | `backend/apps/ai/rag.py:406` |
| **Response Format** | `JSON Object` | `backend/apps/ai/rag.py:407` |
| **사용 목적** | RAG 검색 실패 시 일반 여행 추천 생성 | `backend/apps/ai/rag.py:373-434` |

### 코드 구현

```python
# backend/apps/ai/rag.py (라인 400-407)

def _generate_gpt_recommendations(self, query: str, limit: int = 5) -> List[Dict]:
    """
    GPT-4로 일반적인 여행 추천 생성 (최후의 수단)
    """
    response = openai.chat.completions.create(
        model="gpt-4-turbo-preview",              # ← GPT-4 모델
        messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_prompt}
        ],
        temperature=0.7,                          # ← 창의성 조절
        max_tokens=2000,                          # ← 최대 토큰
        response_format={"type": "json_object"}   # ← JSON 응답 강제
    )
```

### System Prompt

```python
system_prompt = f"""
당신은 여행 전문가입니다. 사용자의 여행 요청에 대해 {limit}개의 추천 여행 코스를 생성하세요.

응답은 반드시 다음 JSON 형식을 따라야 합니다:
{{
    "recommendations": [
        {{
            "title": "추천 제목 (예: 서울 3박 4일 맛집 투어)",
            "channel": "생성된 추천",
            "description": "상세 설명 (200-300자)",
            "schedule": "Day 1: ... Day 2: ... Day 3: ...",
            "url": "https://www.google.com/search?q=여행+추천+검색어",
            "views_num": 0,
            "similarity": 0.75
        }}
    ]
}}
"""
```

---

## 4. 전체 AI 시스템 구조

### 데이터 흐름

```
사용자 입력 (@봇 제주도 맛집 추천해줘)
    ↓
┌─────────────────────────────────────────────────┐
│ 1. Agent LLM (gpt-4-turbo-preview, temp=0.7)   │
│    - 의도 파악 (Intent Detection)                │
│    - 도구 선택 (Tool Selection)                  │
│    - 대화 관리 (Conversation Management)         │
└─────────────────────────────────────────────────┘
    ↓ (recommend_similar_trips 도구 호출)
┌─────────────────────────────────────────────────┐
│ 2. RAG Embedding (text-embedding-ada-002)       │
│    - 쿼리 텍스트 → 1536차원 벡터 변환            │
└─────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────┐
│ 3. pgvector 검색 (CosineDistance)                │
│    - DB에서 유사한 여행 코스 검색                 │
└─────────────────────────────────────────────────┘
    ↓ (결과 있으면 반환)
    ✅ 성공!

    ↓ (결과 없으면 Fallback)
┌─────────────────────────────────────────────────┐
│ 4. Fallback GPT-4 (gpt-4-turbo-preview, 0.7)   │
│    - JSON 형식으로 일반 여행 추천 생성            │
└─────────────────────────────────────────────────┘
    ↓
    ✅ Fallback 결과 반환
```

---

## 5. Temperature 설정 비교

### Temperature란?

**Temperature**는 AI 모델의 **창의성(무작위성)**을 조절하는 파라미터입니다.

| Temperature | 특징 | 사용 사례 |
|-------------|------|-----------|
| **0.0** | 가장 결정론적 (Deterministic) | 사실 확인, 번역, 코드 생성 |
| **0.3 - 0.5** | 일관성 높음, 약간의 변화 | 고객 서비스, FAQ 응답 |
| **0.7** (기본값) | **균형잡힌 창의성** | 일반 대화, 추천, 콘텐츠 생성 |
| **0.9 - 1.0** | 매우 창의적, 다양한 출력 | 브레인스토밍, 시/소설 작성 |
| **1.5 - 2.0** | 극도로 무작위 (거의 사용 안 함) | 실험적 용도 |

### Triplan에서 0.7을 선택한 이유

```python
temperature = 0.7  # ← 왜 0.7인가?
```

| 요구사항 | Temperature 0.7의 장점 |
|---------|----------------------|
| **여행 추천의 다양성** | 같은 질문에도 조금씩 다른 추천 제공 |
| **일관성 유지** | 너무 무작위하지 않아 신뢰성 있음 |
| **사용자 경험** | 반복 질문 시 지루하지 않게 변화 제공 |
| **OpenAI 권장** | 대화형 AI의 권장 기본값 |

### Temperature 변경 방법

```bash
# .env 파일에서 조정 가능
CHAT_AGENT_TEMPERATURE=0.5  # 더 일관적인 응답
CHAT_AGENT_TEMPERATURE=0.9  # 더 창의적인 응답
```

---

## 6. 모델별 비용 비교 (2025년 1월 기준)

### OpenAI API 가격

| 모델 | Input (1M tokens) | Output (1M tokens) | 용도 |
|------|-------------------|---------------------|------|
| **gpt-4-turbo-preview** | $10.00 | $30.00 | Agent, Fallback |
| **text-embedding-ada-002** | $0.10 | - | RAG Embedding |
| **gpt-3.5-turbo** | $0.50 | $1.50 | (대안, 저렴) |

### 실제 비용 예시 (1회 요청)

```python
# 사용자: "@봇 제주도 맛집 추천해줘"

# 1. Agent LLM (gpt-4-turbo-preview)
Input:  500 tokens (System prompt + 대화 이력)  = $0.005
Output: 200 tokens (응답 텍스트)              = $0.006
                                        합계: $0.011

# 2. RAG Embedding (text-embedding-ada-002)
Input:  50 tokens ("제주도 맛집 추천")         = $0.000005
                                        합계: $0.000005

# 3. Fallback GPT-4 (결과 없을 시만)
Input:  300 tokens (System prompt)            = $0.003
Output: 800 tokens (JSON 추천 5개)            = $0.024
                                        합계: $0.027

# 전체 요청당 비용: $0.011 ~ $0.038 (Fallback 여부에 따라)
```

### 월간 비용 추정

```python
# 가정: 일일 활성 사용자 100명, 1인당 10회 요청

일일 요청: 100 * 10 = 1,000회
월간 요청: 1,000 * 30 = 30,000회

# RAG 성공률 80% 가정
RAG 성공: 30,000 * 0.8 * $0.011 = $264
Fallback: 30,000 * 0.2 * $0.038 = $228

월간 총 비용: $264 + $228 = $492
```

---

## 7. Agent Executor 설정

### LangChain Agent 파라미터

```python
# backend/apps/chat/agent.py (라인 55-62)

self.agent_executor = AgentExecutor(
    agent=self.agent,
    tools=self.tools,
    memory=self.memory,
    verbose=True,              # ← 디버깅 로그 출력
    max_iterations=5,          # ← 최대 도구 호출 횟수
    handle_parsing_errors=True # ← 파싱 에러 자동 복구
)
```

| 파라미터 | 값 | 설명 |
|---------|-----|------|
| `verbose` | `True` | Agent 실행 과정을 로그로 출력 (디버깅용) |
| `max_iterations` | `5` | 한 요청에서 최대 5번까지 도구 호출 가능 |
| `handle_parsing_errors` | `True` | LLM 출력 파싱 실패 시 자동으로 재시도 |

### max_iterations 예시

```python
# 사용자: "@봇 제주도 3일 일정 짜주고 각 일차에 맛집 추가해줘"

# Iteration 1: get_planner_info() - 현재 플래너 정보 확인
# Iteration 2: add_place_to_day(day_no=1, place_name="돈사돈")
# Iteration 3: add_place_to_day(day_no=2, place_name="우진해장국")
# Iteration 4: add_place_to_day(day_no=3, place_name="올레국수")
# Iteration 5: 최종 응답 생성

# 만약 6번째 도구 호출이 필요하면 → 중단하고 "너무 복잡한 요청입니다" 반환
```

---

## 8. ChatRequest 저장 시 모델 정보

### 저장 예시

```json
{
  "request_idx": 42,
  "room_idx": 7,
  "user_idx": 4,
  "trip_idx": 5,

  "user_message": "@봇 제주도 맛집 추천해줘",
  "agent_response": "🍜 제주도 맛집 추천 결과:\n\n1. **돈사돈 제주점** ...",

  "tools_used": ["get_planner_info", "recommend_similar_trips"],
  "execution_time_ms": 1420,

  "request_type": "recommend",
  "success": true,
  "error_message": null,

  "created_at": "2025-01-18T14:23:45.123Z",

  // ⚠️ 현재는 모델 정보가 저장되지 않음! (향후 추가 권장)
  // "model_used": "gpt-4-turbo-preview",
  // "temperature": 0.7,
  // "embedding_model": "text-embedding-ada-002",
  // "fallback_used": false
}
```

### 향후 개선 제안

현재 `ChatRequest` 모델에는 **어떤 모델을 사용했는지 기록되지 않습니다**.
나중에 모델을 변경했을 때 과거 요청이 어떤 모델로 처리되었는지 추적이 어렵습니다.

**추천 필드 추가:**

```python
# backend/apps/chat/models.py (ChatRequest 모델에 추가)

class ChatRequest(models.Model):
    # ... 기존 필드 ...

    # AI 모델 정보 (추가 권장)
    llm_model = models.CharField(
        max_length=100,
        default='gpt-4-turbo-preview',
        help_text='Agent LLM model used (e.g., gpt-4-turbo-preview, gpt-3.5-turbo)'
    )
    llm_temperature = models.FloatField(
        default=0.7,
        help_text='Temperature parameter used'
    )
    embedding_model = models.CharField(
        max_length=100,
        default='text-embedding-ada-002',
        null=True,
        blank=True,
        help_text='Embedding model used for RAG (if applicable)'
    )
    fallback_used = models.BooleanField(
        default=False,
        help_text='Whether fallback GPT-4 generation was used'
    )
```

**저장 코드 수정:**

```python
# backend/apps/chat/agent.py (agent 실행 후)

from django.conf import settings

ChatRequest.objects.create(
    # ... 기존 필드 ...
    llm_model=settings.CHAT_AGENT_MODEL,
    llm_temperature=float(settings.CHAT_AGENT_TEMPERATURE),
    embedding_model='text-embedding-ada-002',
    fallback_used=False  # RAG에서 fallback 여부 전달받아야 함
)
```

---

## 9. 환경변수 전체 정리

### .env 파일 예시

```bash
# ==================== OpenAI API ====================
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxx

# ==================== Chat Agent 설정 ====================
# Agent LLM 모델 (기본값: gpt-4-turbo-preview)
CHAT_AGENT_MODEL=gpt-4-turbo-preview

# Temperature (0.0 ~ 2.0, 기본값: 0.7)
CHAT_AGENT_TEMPERATURE=0.7

# ==================== 기타 AI API (Fallback용) ====================
# YouTube Data API v3 (선택)
YOUTUBE_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxx

# Naver Blog API (선택)
NAVER_CLIENT_ID=xxxxxxxxxxxxxx
NAVER_CLIENT_SECRET=xxxxxxxxxxxxxx
```

---

## 10. 모델 변경 가이드

### gpt-4-turbo-preview → gpt-3.5-turbo (비용 절감)

```bash
# .env 파일 수정
CHAT_AGENT_MODEL=gpt-3.5-turbo
CHAT_AGENT_TEMPERATURE=0.7
```

**장점:**
- 비용 95% 절감 ($10 → $0.50 per 1M tokens)
- 응답 속도 2~3배 빠름

**단점:**
- 복잡한 추론 능력 감소
- 도구 선택 정확도 하락 가능성
- 한국어 품질 약간 저하

### gpt-4-turbo-preview → gpt-4 (최신 모델)

```bash
# .env 파일 수정
CHAT_AGENT_MODEL=gpt-4-0125-preview  # 2025년 최신
CHAT_AGENT_TEMPERATURE=0.7
```

**장점:**
- 최신 학습 데이터 (2023년 12월까지)
- 개선된 추론 능력
- 더 나은 한국어 지원

**단점:**
- 비용 동일하거나 약간 증가

### Temperature 실험

```bash
# 더 일관적인 응답 (추천: 고객 서비스 봇)
CHAT_AGENT_TEMPERATURE=0.3

# 기본값 (추천: 일반 대화)
CHAT_AGENT_TEMPERATURE=0.7

# 더 창의적인 응답 (추천: 콘텐츠 생성)
CHAT_AGENT_TEMPERATURE=0.9
```

---

## 11. 모니터링 및 디버깅

### 로그 확인

```python
# LLM 호출 로그 확인
logger.info(f"🤖 Using model: {settings.CHAT_AGENT_MODEL}")
logger.info(f"🌡️ Temperature: {settings.CHAT_AGENT_TEMPERATURE}")

# RAG 임베딩 로그
logger.info(f"✅ Created embedding for query: '{query[:50]}...'")

# Fallback 사용 여부
logger.warning(f"⚠️ No RAG results, trying fallback...")
logger.info(f"✅ GPT-4 generated {len(gpt_results)} recommendations")
```

### Docker 로그 확인

```bash
# Backend 컨테이너 로그 실시간 확인
docker-compose logs -f backend

# Agent 관련 로그만 필터링
docker-compose logs backend | grep "TravelPlannerAgent"

# RAG 관련 로그만 필터링
docker-compose logs backend | grep "TripRAG"
```

---

## 12. 참고 자료

### 관련 파일

| 파일 | 설명 |
|------|------|
| `backend/config/settings/base.py:175-180` | AI 모델 환경변수 설정 |
| `backend/apps/chat/agent.py:28-62` | Agent LLM 초기화 |
| `backend/apps/ai/rag.py:24-48` | RAG 임베딩 모델 |
| `backend/apps/ai/rag.py:400-407` | Fallback GPT-4 설정 |
| `backend/apps/chat/models.py:85-166` | ChatRequest 모델 |

### OpenAI 공식 문서

- **GPT-4 Turbo**: https://platform.openai.com/docs/models/gpt-4-turbo-and-gpt-4
- **Embeddings**: https://platform.openai.com/docs/guides/embeddings
- **Temperature**: https://platform.openai.com/docs/api-reference/chat/create#temperature
- **Pricing**: https://openai.com/pricing

### LangChain 공식 문서

- **ChatOpenAI**: https://python.langchain.com/docs/integrations/chat/openai
- **Agent Executor**: https://python.langchain.com/docs/modules/agents/agent_types/

---

## 요약

| 항목 | 모델 | Temperature | 용도 |
|------|------|-------------|------|
| **Agent LLM** | `gpt-4-turbo-preview` | `0.7` | 채팅 봇 주 모델 (도구 선택, 대화) |
| **RAG Embedding** | `text-embedding-ada-002` | N/A | 쿼리 텍스트 → 1536차원 벡터 변환 |
| **Fallback GPT-4** | `gpt-4-turbo-preview` | `0.7` | RAG 결과 없을 시 일반 추천 생성 |

**핵심 포인트:**
- 모든 설정은 환경변수 (.env)로 조정 가능
- Temperature 0.7 = 창의성과 일관성의 균형
- ChatRequest에 모델 정보 저장 권장 (향후 추적 용이)
- 비용 절감 필요 시 gpt-3.5-turbo로 변경 검토

---

**생성일:** 2025-01-18
**작성자:** Claude Code
**버전:** 1.0
