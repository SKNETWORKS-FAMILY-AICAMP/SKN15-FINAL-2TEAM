# 🚀 Triplan 프로젝트 배포 가이드

## 📋 필수 사전 준비

### 1. **시스템 요구사항**

```bash
- OS: Ubuntu 20.04+ / macOS / Windows (WSL2)
- Docker: 20.10+
- Docker Compose: 2.0+
- RAM: 최소 8GB (권장 16GB)
- Disk: 최소 20GB 여유 공간
```

### 2. **Docker 설치 확인**

```bash
# Docker 버전 확인
docker --version
# Docker version 24.0.0, build ...

# Docker Compose 버전 확인
docker-compose --version
# Docker Compose version v2.20.0
```

---

## 🔧 초기 설정

### 1. **프로젝트 클론**

```bash
git clone https://github.com/SKNETWORKS-FAMILY-AICAMP/SKN15-FINAL-2TEAM.git
cd SKN15-FINAL-2TEAM
```

### 2. **환경변수 설정**

`.env` 파일을 생성하고 다음 내용을 입력하세요:

```bash
# Database
POSTGRES_DB=lecun2
POSTGRES_USER=postgres
POSTGRES_PASSWORD=lecun123!@#

# Django
DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production
ALLOWED_HOSTS=*

# API Keys
OPENAI_API_KEY=sk-your-openai-api-key
KAKAO_REST_API_KEY=your-kakao-rest-api-key
KAKAO_MAP_API_KEY=your-kakao-map-api-key
NEXT_PUBLIC_KAKAO_API_KEY=your-kakao-javascript-key

# Airflow
AIRFLOW_FERNET_KEY=generate-with-python-cryptography-fernet
AIRFLOW_WEBSERVER_SECRET_KEY=generate-random-secret
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=admin
```

**Fernet Key 생성 방법**:
```bash
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

### 3. **STT 모델 파일 준비** ⚠️ 중요!

Git에서는 용량 제한으로 STT 모델 파일이 제외되어 있습니다.

```bash
# 모델 디렉토리 생성
mkdir -p backend/models/stt/

# 모델 파일을 다운로드하거나 복사
# 필요한 파일:
# - backend/models/stt/model.safetensors (922MB)
# - backend/models/stt/config.json
# - backend/models/stt/generation_config.json
# - backend/models/stt/preprocessor_config.json
```

**모델 파일이 없으면 STT 기능이 작동하지 않습니다!**

---

## 🐳 Docker 실행

### 1. **빌드 및 실행**

```bash
# 1. Docker 이미지 빌드 (최초 1회, 10-15분 소요)
docker-compose build

# 2. 컨테이너 실행
docker-compose up -d

# 3. 상태 확인
docker-compose ps
```

**정상 실행 시 출력 예시**:
```
NAME                 STATUS
triplan-backend      Up (healthy)
triplan-db           Up (healthy)
triplan-redis        Up (healthy)
triplan-frontend     Up
triplan-websocket    Up (healthy)
triplan-nginx        Up
triplan-airflow-*    Up
```

### 2. **로그 확인**

```bash
# 전체 로그
docker-compose logs -f

# 특정 서비스 로그
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f nginx
```

---

## ⚠️ 502 Bad Gateway 에러 해결

### 원인 1: 백엔드 컨테이너가 시작되지 않음

**증상**:
```bash
docker-compose ps
# triplan-backend    Exited (1)
```

**해결 방법**:
```bash
# 1. 백엔드 로그 확인
docker-compose logs backend

# 2. 에러 확인 (주로 환경변수 누락)
# - OPENAI_API_KEY 누락
# - DATABASE_URL 오류
# - migration 실패
```

### 원인 2: 데이터베이스 연결 실패

**증상**:
```
django.db.utils.OperationalError: could not connect to server
```

**해결 방법**:
```bash
# 1. PostgreSQL 상태 확인
docker-compose ps postgres
# triplan-db    Up (healthy)

# 2. 데이터베이스 재시작
docker-compose restart postgres

# 3. 백엔드 재시작
docker-compose restart backend
```

### 원인 3: 포트 충돌

**증상**:
```
Error: port is already allocated
```

**해결 방법**:
```bash
# 1. 포트 사용 중인 프로세스 확인
sudo lsof -i :8000
sudo lsof -i :3000
sudo lsof -i :5432

# 2. 프로세스 종료
sudo kill -9 <PID>

# 3. Docker 재시작
docker-compose down
docker-compose up -d
```

### 원인 4: 메모리 부족

**증상**:
```
docker: Error response from daemon: OCI runtime create failed
```

**해결 방법**:
```bash
# 1. Docker 메모리 설정 확인
docker info | grep Memory

# 2. Docker Desktop 설정에서 메모리 증가 (8GB → 12GB)

# 3. 불필요한 컨테이너 정리
docker system prune -a
```

### 원인 5: STT 모델 파일 누락

**증상**:
```
FileNotFoundError: [Errno 2] No such file or directory: './models/stt/model.safetensors'
```

**해결 방법**:
```bash
# 1. 모델 파일 확인
ls -lh backend/models/stt/

# 2. 모델 파일 복사 (Google Drive, S3 등에서)
# 파일 크기: model.safetensors (922MB)

# 3. 백엔드 재시작
docker-compose restart backend
```

---

## 🔍 Health Check 확인

### 1. **Backend Health Check**

```bash
curl http://localhost:8000/admin/
# 200 OK → 정상
```

### 2. **Frontend Health Check**

```bash
curl http://localhost:3000/
# 200 OK → 정상
```

### 3. **WebSocket Health Check**

```bash
curl http://localhost:8001/
# WebSocket 연결 확인
```

### 4. **Nginx Health Check**

```bash
curl http://localhost/
# 200 OK → 정상
# 502 Bad Gateway → 백엔드 문제
```

---

## 🗄️ 데이터베이스 초기화

### 1. **Migration 실행**

```bash
# 컨테이너 내부 접속
docker exec -it triplan-backend bash

# Migration 실행
python manage.py migrate

# 슈퍼유저 생성
python manage.py createsuperuser

# 정적 파일 수집
python manage.py collectstatic --noinput
```

### 2. **초기 데이터 로드** (선택)

```bash
# Fixture 로드
python manage.py loaddata initial_data.json

# CSV 임포트 (국가, 지역 데이터)
python manage.py import_csv
```

---

## 🌐 접속 URL

### 1. **프론트엔드**
- http://localhost (Nginx를 통한 접근)
- http://localhost:3000 (직접 접근)

### 2. **백엔드 API**
- http://localhost:8000/admin/ (Django Admin)
- http://localhost:8000/api/ (REST API)

### 3. **Airflow**
- http://localhost:8080 (Airflow 웹 UI)
- 기본 계정: admin / admin

### 4. **데이터베이스**
- Host: localhost
- Port: 5432
- Database: lecun2
- User: postgres
- Password: lecun123!@#

---

## 🧹 정리 및 재시작

### 1. **컨테이너 중지**

```bash
docker-compose down
```

### 2. **볼륨까지 모두 삭제** (주의!)

```bash
# ⚠️ 데이터베이스 데이터도 삭제됨!
docker-compose down -v
```

### 3. **재빌드 및 재시작**

```bash
# 캐시 없이 완전 재빌드
docker-compose build --no-cache

# 재시작
docker-compose up -d
```

### 4. **로그 초기화**

```bash
# Airflow 로그 삭제
rm -rf airflow/logs/*

# Docker 로그 초기화
docker-compose down
docker system prune -f
docker-compose up -d
```

---

## 📊 디버깅 팁

### 1. **컨테이너 내부 접속**

```bash
# Backend
docker exec -it triplan-backend bash

# Frontend
docker exec -it triplan-frontend sh

# PostgreSQL
docker exec -it triplan-db psql -U postgres -d lecun2
```

### 2. **네트워크 확인**

```bash
# Docker 네트워크 확인
docker network ls

# 특정 네트워크 상세 정보
docker network inspect skn15-final-2team_triplan-network
```

### 3. **볼륨 확인**

```bash
# 볼륨 목록
docker volume ls

# 볼륨 상세 정보
docker volume inspect skn15-final-2team_postgres-data
```

### 4. **리소스 사용량 확인**

```bash
# 실시간 모니터링
docker stats

# 디스크 사용량
docker system df
```

---

## 🔒 보안 설정 (Production)

### 1. **환경변수 보안**

```bash
# .env 파일 권한 제한
chmod 600 .env

# Git에 추가하지 않기 (.gitignore 확인)
echo ".env" >> .gitignore
```

### 2. **Django 설정**

`backend/config/settings/production.py`:
```python
DEBUG = False
ALLOWED_HOSTS = ['your-domain.com', 'www.your-domain.com']
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
```

### 3. **데이터베이스 비밀번호 변경**

```bash
# PostgreSQL 비밀번호 변경
docker exec -it triplan-db psql -U postgres
ALTER USER postgres PASSWORD 'new-strong-password';
\q

# .env 파일 업데이트
POSTGRES_PASSWORD=new-strong-password
```

### 4. **Nginx HTTPS 설정**

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;

    # ... 기타 설정
}
```

---

## 📈 성능 최적화

### 1. **Gunicorn Workers 조정**

`docker-compose.yml`:
```yaml
backend:
  command: >
    gunicorn config.wsgi:application
    --bind 0.0.0.0:8000
    --workers 4  # CPU 코어 수 * 2
    --threads 2
    --timeout 300
```

### 2. **Redis 메모리 설정**

`docker-compose.yml`:
```yaml
redis:
  command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
```

### 3. **PostgreSQL 튜닝**

`database/postgresql.conf`:
```
shared_buffers = 256MB
effective_cache_size = 1GB
max_connections = 100
```

---

## 🆘 문제 해결 체크리스트

### ✅ 502 Bad Gateway 발생 시

1. [ ] 백엔드 컨테이너 상태 확인: `docker-compose ps backend`
2. [ ] 백엔드 로그 확인: `docker-compose logs backend | tail -50`
3. [ ] 데이터베이스 연결 확인: `docker exec triplan-db pg_isready`
4. [ ] 환경변수 확인: `.env` 파일에 모든 필수 키 존재
5. [ ] 포트 충돌 확인: `sudo lsof -i :8000`
6. [ ] STT 모델 파일 존재 확인: `ls backend/models/stt/model.safetensors`
7. [ ] 컨테이너 재시작: `docker-compose restart backend nginx`

### ✅ 프론트엔드 접속 안됨

1. [ ] 프론트엔드 컨테이너 상태: `docker-compose ps frontend`
2. [ ] 빌드 에러 확인: `docker-compose logs frontend`
3. [ ] Next.js 빌드 완료 확인 (로그에 "ready" 메시지)
4. [ ] 환경변수 확인: `NEXT_PUBLIC_KAKAO_API_KEY`
5. [ ] 포트 접속: `curl http://localhost:3000`

### ✅ WebSocket 연결 실패

1. [ ] WebSocket 컨테이너 상태: `docker-compose ps websocket`
2. [ ] Daphne 실행 확인: `docker-compose logs websocket`
3. [ ] Redis 연결 확인: `docker exec triplan-redis redis-cli ping`
4. [ ] Nginx WebSocket 프록시 설정 확인

---

## 📚 추가 리소스

### 공식 문서
- Django: https://docs.djangoproject.com/
- Next.js: https://nextjs.org/docs
- Docker: https://docs.docker.com/
- PostgreSQL: https://www.postgresql.org/docs/

### 프로젝트 문서
- [audio_아키텍처.md](audio_아키텍처.md) - STT 시스템 구조
- [RAG_아키텍처.md](RAG_아키텍처.md) - RAG 시스템 구조

---

## 💬 지원

문제가 해결되지 않을 경우:

1. **GitHub Issues**: https://github.com/SKNETWORKS-FAMILY-AICAMP/SKN15-FINAL-2TEAM/issues
2. **로그 첨부**: `docker-compose logs > logs.txt`
3. **환경 정보**: OS, Docker 버전, RAM 용량

---

**작성일**: 2025-11-06
**버전**: 1.0.0
**작성자**: Claude Code
