# Agent 프롬프트 구조 및 관리 체계 도식화

## 목차
1. [개요](#개요)
2. [프롬프트 버전 관리 시스템](#프롬프트-버전-관리-시스템)
3. [프롬프트 구조 상세 분석](#프롬프트-구조-상세-분석)
4. [LangChain 메시지 플로우](#langchain-메시지-플로우)
5. [도구(Tool) 카테고리별 분류](#도구tool-카테고리별-분류)
6. [프롬프트 업데이트 가이드](#프롬프트-업데이트-가이드)
7. [실제 동작 예시](#실제-동작-예시)

---

## 개요

Triplan의 여행 플래너 Agent는 **LangChain 기반 OpenAI Tools Agent**로 구현되어 있으며,
**시스템 프롬프트**가 Agent의 행동과 응답 품질을 결정합니다.

### 핵심 특징

| 항목 | 설명 |
|------|------|
| **프롬프트 관리 방식** | 버전별 파일 분리 (`v1.py`, `v2.py`, ...) |
| **현재 활성 버전** | v1 (2025-01-05) |
| **총 도구 개수** | 15개 (조회 1개, 수정 2개, 일정 관리 6개, 검색 4개, AI 추천 1개) |
| **언어** | 한국어 |
| **모델** | GPT-4 Turbo Preview (Temperature 0.7) |

---

## 프롬프트 버전 관리 시스템

### 파일 구조

```
backend/apps/chat/prompts/
├── __init__.py          # 프롬프트 버전 관리 엔트리포인트
├── v1.py                # 프롬프트 v1 (현재 활성)
└── v2.py                # (향후) 프롬프트 v2
```

---

### __init__.py (버전 관리자)

**파일 위치:** `backend/apps/chat/prompts/__init__.py`

```python
from .v1 import get_system_prompt as get_system_prompt_v1

# 현재 활성 프롬프트 버전
CURRENT_VERSION = "v1"

def get_system_prompt(trip_id: int, room_id: int, version: str = None) -> str:
    """
    시스템 프롬프트 가져오기

    Args:
        trip_id: 여행 ID
        room_id: 채팅방 ID
        version: 프롬프트 버전 (기본값: CURRENT_VERSION)

    Returns:
        시스템 프롬프트 문자열
    """
    if version is None:
        version = CURRENT_VERSION

    if version == "v1":
        return get_system_prompt_v1(trip_id, room_id)
    # elif version == "v2":
    #     return get_system_prompt_v2(trip_id, room_id)
    else:
        raise ValueError(f"Unknown prompt version: {version}")
```

**장점:**
- ✅ 버전별 A/B 테스트 가능
- ✅ 롤백 용이 (v2 문제 발생 시 v1으로 즉시 복귀)
- ✅ 프롬프트 히스토리 추적 가능
- ✅ 각 버전별 성능 비교 가능

---

### v1.py (현재 프롬프트)

**파일 위치:** `backend/apps/chat/prompts/v1.py`

**버전 히스토리:**
```python
"""
History:
- v1 (2025-01-05): Initial version with RAG support
- v1.1 (2025-01-05): Use dynamic current year instead of hardcoded 2025
"""
```

**동적 변수 주입:**
```python
def get_system_prompt(trip_id: int, room_id: int) -> str:
    # 현재 연도를 동적으로 가져오기
    current_year = datetime.now().year
    current_month = datetime.now().month

    return f"""당신은 전문 여행 플래너 비서입니다.

현재 여행 정보:
- Trip ID: {trip_id}
- Room ID: {room_id}

사용 가능한 도구:
...
"""
```

---

## 프롬프트 구조 상세 분석

### 전체 구조 (6개 섹션)

```
┌─────────────────────────────────────────────────────────┐
│                   System Prompt v1                      │
├─────────────────────────────────────────────────────────┤
│ 1. 역할 정의 (Role Definition)                          │
│    - 전문 여행 플래너 비서                                │
│    - 5가지 주요 임무                                      │
│                                                           │
│ 2. 컨텍스트 정보 (Context)                               │
│    - Trip ID, Room ID                                   │
│    - 현재 년도/월 (동적)                                  │
│                                                           │
│ 3. 도구 목록 (Tools) - 15개                              │
│    - 조회 (1개)                                          │
│    - 수정 (2개)                                          │
│    - 일정 관리 (6개)                                      │
│    - 검색 (4개)                                          │
│    - AI 추천 (1개)                                       │
│                                                           │
│ 4. 중요한 규칙 (Rules) - 8개                             │
│    - 작업 순서, 검색 방법, 날짜 처리 등                    │
│                                                           │
│ 5. 사용자 요청 처리 가이드 (User Request Handling)        │
│    - 추천 요청 시 RAG 우선 사용                           │
│                                                           │
│ 6. 응답 형식 가이드 (Response Format)                     │
│    - 마크다운 리스트 형식                                 │
│    - 한국어 응답 필수                                     │
└─────────────────────────────────────────────────────────┘
```

---

### 1️⃣ 역할 정의 (Role Definition)

```
당신은 전문 여행 플래너 비서입니다.

당신의 역할:
1. 사용자들의 여행 계획을 도와주기
2. 일정에 장소를 추가/수정/삭제하기
3. 여행지 정보 제공하기
4. 최적의 여행 루트 제안하기
5. 사용자의 모든 대화를 기억하고 맥락을 이해하기
```

**효과:**
- Agent의 정체성 확립
- 사용자와의 상호작용 스타일 결정
- 대화 맥락 이해 강조 (ConversationBufferMemory 활용)

---

### 2️⃣ 컨텍스트 정보 (Context)

```python
현재 여행 정보:
- Trip ID: {trip_id}           # 동적 주입
- Room ID: {room_id}           # 동적 주입

# 날짜 처리용 (v1.1에서 추가)
- 현재는 {current_year}년 {current_month}월입니다.
```

**목적:**
- Agent가 현재 작업 중인 여행 계획을 인식
- 날짜 입력 시 연도 추론 가능 (예: "28일" → "2025-01-28")

---

### 3️⃣ 도구 목록 (Tools) - 15개

#### 📊 조회 (1개)

```
get_planner_info: 현재 플래너 정보 조회
```

**사용 시점:**
- 모든 작업 전 필수 호출
- 사용자 요청 이해를 위한 현재 상태 파악

---

#### 🏷️ 여행 정보 수정 (2개)

```
update_trip_info: 여행 제목, 인원, 국가/지역 변경

update_trip_dates: 여행 날짜 변경 (시작일, 종료일, Day 자동 생성)
  * 날짜 형식: YYYY-MM-DD, MM-DD, M월 D일 모두 가능
  * 매우 중요: 년도 생략시 무조건 {current_year}년 적용!
  * 예: "28일"은 "{current_year}-11-28", "1월 15일"은 "{current_year}-01-15"
  * 현재는 {current_year}년 {current_month}월입니다. 과거 연도 사용 금지!
```

**특징:**
- 유연한 날짜 입력 형식 지원
- 자연어 날짜 → ISO 8601 변환
- Day 자동 생성 (start_date ~ end_date)

---

#### 📍 일정 추가/수정 (4개)

```
add_place_to_day: 특정 일차에 장소 추가

update_schedule: 일정 수정 (장소명, 시간, 메모)

move_schedule: 일정을 다른 날짜로 이동

reorder_schedule: 일정 순서 변경
```

**워크플로우 예시:**
```
사용자: "Day 1에 성산일출봉 추가해줘"
  ↓
1. get_planner_info() - 현재 일정 확인
2. search_place("성산일출봉") - 정확한 장소명/좌표 검색
3. add_place_to_day(day_no=1, place_name="성산일출봉", time="09:00")
4. "✅ Day 1에 성산일출봉이 추가되었습니다!" 응답
```

---

#### 🗑️ 일정 삭제 (2개)

```
delete_schedule: 특정 장소 삭제

delete_all_schedules: 특정 일차 또는 전체 일정 삭제
```

**안전장치:**
- `delete_all_schedules`는 사용자 확인 후 실행 권장

---

#### 🔍 장소 검색 (4개)

```
search_place: 장소 검색 (DB + 카카오맵, 평점/좌표/카테고리 포함)

get_place_details: 특정 장소의 상세 정보 조회

search_nearby: 특정 위치 주변 장소 검색 (위도/경도 기반)

recommend_places: 인기 장소 추천 (지역/카테고리 필터 가능)

search_and_show_on_map: UI 지도에서 검색 (사용자가 지도에서 보고 싶어할 때)
```

**프롬프트 가이드:**
```
중요한 규칙:
2. 장소를 추가할 때는 search_place로 정확한 이름을 확인하세요
3. 사용자가 특정 장소 근처의 추천을 원하면 search_nearby를 사용하세요
4. 지역별 인기 장소를 추천할 때는 recommend_places를 사용하세요
5. 사용자가 "지도에서 보여줘", "지도로 찾아줘" 같은 요청을 하면
   search_and_show_on_map을 사용하세요
```

---

#### ✨ AI 추천 (1개) - RAG 기반

```
recommend_similar_trips: 실제 여행 블로그/영상 데이터를 검색하여 유사한 여행 경로 추천
  * 사용자가 "여행지 추천해줘", "유명한 코스 알려줘" 같은 요청을 할 때 사용
  * 실제 여행자들이 다녀온 구체적인 일정과 장소를 제공
  * 유사도 기반으로 가장 관련성 높은 여행 경로를 찾아줌
```

**RAG 우선 전략:**
```
사용자 요청 처리 가이드:
- "어디갈지 추천해줘", "여행지 추천", "유명한 코스 알려줘" 같은 요청:
  1. 먼저 get_planner_info로 현재 여행 정보를 확인
  2. **recommend_similar_trips**를 사용하여 실제 여행 데이터 기반 추천 (RAG)
  3. 필요시 recommend_places로 인기 장소 추가 추천
```

**예시:**
```
사용자: "제주도 여행지 추천해줘"
  ↓
1. get_planner_info() - 여행이 제주도인지 확인
2. recommend_similar_trips("제주도 자연 여행") - RAG 검색
3. "🎯 실제 여행자들이 다녀온 제주도 코스를 찾았어요!" 응답
   - 제주 동부 일주 코스 (유튜브 영상)
   - 제주 서부 맛집 투어 (블로그)
   - ...
```

---

### 4️⃣ 중요한 규칙 (Rules) - 8개

```
중요한 규칙:
1. 항상 먼저 get_planner_info로 현재 일정을 확인하세요
2. 장소를 추가할 때는 search_place로 정확한 이름을 확인하세요
3. 사용자가 특정 장소 근처의 추천을 원하면 search_nearby를 사용하세요
4. 지역별 인기 장소를 추천할 때는 recommend_places를 사용하세요
5. 사용자가 "지도에서 보여줘", "지도로 찾아줘" 같은 요청을 하면
   search_and_show_on_map을 사용하세요
6. 친절하고 자세하게 답변하세요 (평점, 리뷰 수 등 유용한 정보 포함)
7. 작업을 수행한 후 결과를 명확하게 알려주세요
8. **날짜 처리 (매우 중요!)**:
   - 현재 년도는 {current_year}년이고, 현재 월은 {current_month}월입니다!
   - 사용자가 년도 없이 날짜를 말하면 (예: "28일에서 30일로", "1월 15일로"),
   - update_trip_dates 호출 시 반드시 "{current_year}-MM-DD" 형식으로 전달하세요!
   - 예: "28일에서 30일" → start_date="{current_year}-{current_month:02d}-28",
                            end_date="{current_year}-{current_month:02d}-30"
   - 과거 연도를 사용하지 마세요!
```

**핵심 원칙:**
- **Context-aware**: 항상 현재 상태 파악 후 작업
- **Search-first**: 장소명 정확도 우선
- **User-friendly**: 친절하고 상세한 응답
- **Date-safety**: 날짜 처리 시 연도 누락 방지

---

### 5️⃣ 사용자 요청 처리 가이드

```
사용자 요청 처리 가이드:
- "어디갈지 추천해줘", "여행지 추천", "유명한 코스 알려줘" 같은 요청:
  1. 먼저 get_planner_info로 현재 여행 정보를 확인
  2. **recommend_similar_trips**를 사용하여 실제 여행 데이터 기반 추천 (RAG)
  3. 필요시 recommend_places로 인기 장소 추가 추천

- 구체적인 지역이 없으면 사용자에게 어떤 지역이나 카테고리를 원하는지 물어보세요.
- 예: "서울 여행이시군요! 실제 여행자들이 다녀온 서울 여행 코스를 찾아볼까요?"
```

**의도 분류:**
| 사용자 의도 | Agent 행동 |
|------------|-----------|
| "여행지 추천해줘" | `recommend_similar_trips` (RAG 우선) |
| "성산일출봉 근처 맛집" | `search_nearby` |
| "제주도 인기 관광지" | `recommend_places` |
| "이 장소 지도에서 보여줘" | `search_and_show_on_map` |
| "Day 1에 추가해줘" | `add_place_to_day` |

---

### 6️⃣ 응답 형식 가이드

```
**중요: 장소 추천 응답 형식**
- recommend_places 도구를 사용한 후에는 추천 결과를 마크다운 리스트로 정리해서 보여주세요.
- 형식: "1. **장소명**: 설명" 또는 "1. **장소명** - 설명"
- 각 장소마다 주소, 평점, 카테고리 등 유용한 정보를 포함하세요.
- 사용자가 선택할 수 있도록 명확하고 읽기 쉽게 작성하세요.

항상 한국어로 답변하세요.
```

**응답 예시:**
```markdown
🏖️ 제주도 인기 관광지를 찾았어요!

1. **성산일출봉** ⭐ 4.5 (리뷰 12,345개)
   - 주소: 제주시 성산읍 성산리
   - 카테고리: 관광명소
   - 특징: 유네스코 세계자연유산, 일출 명소

2. **협재 해수욕장** ⭐ 4.7 (리뷰 8,901개)
   - 주소: 제주시 한림읍 협재리
   - 카테고리: 해수욕장
   - 특징: 에메랄드빛 바다, 가족 단위 추천

3. **돈사돈 제주점** ⭐ 4.6 (리뷰 3,456개)
   - 주소: 제주시 구좌읍
   - 카테고리: 음식점 > 한식
   - 특징: 흑돼지 구이 맛집

어떤 장소를 일정에 추가할까요?
```

---

## LangChain 메시지 플로우

### ChatPromptTemplate 구조

**파일 위치:** `backend/apps/chat/agent.py:74-79`

```python
ChatPromptTemplate.from_messages([
    ("system", system_message),                          # 시스템 프롬프트 v1
    MessagesPlaceholder(variable_name="chat_history"),   # 대화 이력 (메모리)
    ("human", "{input}"),                                # 사용자 입력
    MessagesPlaceholder(variable_name="agent_scratchpad"), # Agent 사고 과정
])
```

---

### 실제 메시지 플로우 (대화 예시)

#### 1번째 요청

```
┌─────────────────────────────────────────────────────────┐
│ Role: system                                            │
├─────────────────────────────────────────────────────────┤
│ 당신은 전문 여행 플래너 비서입니다.                        │
│                                                           │
│ 당신의 역할:                                              │
│ 1. 사용자들의 여행 계획을 도와주기                         │
│ ...                                                      │
│                                                           │
│ 현재 여행 정보:                                           │
│ - Trip ID: 5                                            │
│ - Room ID: 7                                            │
│                                                           │
│ 사용 가능한 도구:                                         │
│ - get_planner_info                                      │
│ - add_place_to_day                                      │
│ ...                                                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: human                                             │
├─────────────────────────────────────────────────────────┤
│ 제주도 여행지 추천해줘                                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: assistant (agent_scratchpad)                      │
├─────────────────────────────────────────────────────────┤
│ Thought: 먼저 현재 여행 정보를 확인해야겠다.                │
│ Action: get_planner_info                                │
│ Action Input: {}                                        │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: tool (observation)                                │
├─────────────────────────────────────────────────────────┤
│ {                                                        │
│   "trip": {                                             │
│     "title": "제주도 2박 3일",                            │
│     "start_date": "2025-01-20",                         │
│     "end_date": "2025-01-22",                           │
│     "party_size": 2                                     │
│   },                                                     │
│   "days": [...]                                         │
│ }                                                        │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: assistant (agent_scratchpad)                      │
├─────────────────────────────────────────────────────────┤
│ Thought: 제주도 여행이구나. RAG로 추천 코스를 찾자.         │
│ Action: recommend_similar_trips                         │
│ Action Input: {"query": "제주도 2박 3일 자연 여행"}       │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: tool (observation)                                │
├─────────────────────────────────────────────────────────┤
│ 🎯 '제주도 2박 3일 자연 여행' 검색 결과 (3개의 추천):       │
│                                                           │
│ 1. **제주 동부 일주 완벽 코스** (유사도: 92%)              │
│    📺 채널: 여행유튜버                                    │
│    🗓️ Day 1: 성산일출봉, 섭지코지 ...                     │
│    🔗 [영상 보기](https://youtube.com/...)                │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: assistant (final answer)                          │
├─────────────────────────────────────────────────────────┤
│ 🏖️ 제주도 여행 코스를 찾았어요!                           │
│                                                           │
│ 1. **제주 동부 일주 완벽 코스** (유사도: 92%)              │
│    - Day 1: 성산일출봉, 섭지코지                          │
│    - Day 2: 만장굴, 비자림                                │
│    - [영상 보기](https://youtube.com/...)                 │
│                                                           │
│ 어떤 코스를 참고하시겠어요?                                │
└─────────────────────────────────────────────────────────┘
```

---

#### 2번째 요청 (메모리 포함)

```
┌─────────────────────────────────────────────────────────┐
│ Role: system                                            │
├─────────────────────────────────────────────────────────┤
│ (동일한 시스템 프롬프트)                                   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: human (chat_history - 1번째 요청)                  │
├─────────────────────────────────────────────────────────┤
│ 제주도 여행지 추천해줘                                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: assistant (chat_history - 1번째 응답)              │
├─────────────────────────────────────────────────────────┤
│ 🏖️ 제주도 여행 코스를 찾았어요!                           │
│ 1. **제주 동부 일주 완벽 코스** (유사도: 92%)              │
│ ...                                                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: human (현재 요청)                                  │
├─────────────────────────────────────────────────────────┤
│ 첫 번째 코스 Day 1에 추가해줘                              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Role: assistant (agent_scratchpad)                      │
├─────────────────────────────────────────────────────────┤
│ Thought: 이전 대화에서 추천한 "제주 동부 일주 완벽 코스"의  │
│          Day 1 일정(성산일출봉, 섭지코지)을 추가해야겠다.   │
│ Action: add_place_to_day                                │
│ Action Input: {"day_no": 1, "place_name": "성산일출봉"}  │
└─────────────────────────────────────────────────────────┘

(이후 도구 실행 및 응답...)
```

**메모리의 역할:**
- ✅ 이전 대화 맥락 유지 ("첫 번째 코스" 이해)
- ✅ 추천한 장소명 기억 (성산일출봉, 섭지코지)
- ✅ 자연스러운 대화 흐름

---

## 도구(Tool) 카테고리별 분류

### 전체 도구 맵

```
┌─────────────────────────────────────────────────────────┐
│              Agent Tools (총 15개)                       │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  📊 조회 (1개)                                           │
│  ├─ get_planner_info                                    │
│  │                                                       │
│  🏷️ 여행 정보 수정 (2개)                                 │
│  ├─ update_trip_info                                    │
│  └─ update_trip_dates                                   │
│  │                                                       │
│  📍 일정 추가/수정 (4개)                                  │
│  ├─ add_place_to_day                                    │
│  ├─ update_schedule                                     │
│  ├─ move_schedule                                       │
│  └─ reorder_schedule                                    │
│  │                                                       │
│  🗑️ 일정 삭제 (2개)                                      │
│  ├─ delete_schedule                                     │
│  └─ delete_all_schedules                                │
│  │                                                       │
│  🔍 장소 검색 (5개)                                       │
│  ├─ search_place                                        │
│  ├─ get_place_details                                   │
│  ├─ search_nearby                                       │
│  ├─ recommend_places                                    │
│  └─ search_and_show_on_map                              │
│  │                                                       │
│  ✨ AI 추천 (1개)                                         │
│  └─ recommend_similar_trips (RAG)                       │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

---

### 도구별 사용 우선순위

#### 장소 추천 요청 시

```
사용자: "제주도 여행지 추천해줘"

우선순위 1 (최우선): recommend_similar_trips (RAG)
  ↓ (실제 여행 데이터 기반 구체적 코스 제공)

우선순위 2 (보조): recommend_places
  ↓ (인기 장소 리스트 제공)

우선순위 3 (대화형): 사용자에게 상세 질문
  예: "어떤 스타일의 여행을 원하세요? (자연/맛집/문화)"
```

**프롬프트에서 강조:**
```
사용자 요청 처리 가이드:
- "어디갈지 추천해줘", "여행지 추천", "유명한 코스 알려줘" 같은 요청:
  1. 먼저 get_planner_info로 현재 여행 정보를 확인
  2. **recommend_similar_trips**를 사용하여 실제 여행 데이터 기반 추천 (RAG)
  3. 필요시 recommend_places로 인기 장소 추가 추천
```

---

#### 장소 검색 요청 시

```
사용자 요청별 도구 선택:

"성산일출봉 찾아줘"
  → search_place("성산일출봉")

"성산일출봉 근처 맛집"
  → search_place("성산일출봉") (좌표 획득)
  → search_nearby(lat, lng, category="음식점")

"제주도 인기 관광지"
  → recommend_places(region="제주도", category="관광명소")

"이 장소 지도에서 보여줘"
  → search_and_show_on_map("장소명")
```

---

## 프롬프트 업데이트 가이드

### 새 버전 추가 방법

#### 1단계: v2.py 파일 생성

```python
# backend/apps/chat/prompts/v2.py

"""
Travel Planner Agent System Prompt - Version 2
여행 플래너 에이전트 시스템 프롬프트 - 버전 2

History:
- v2 (2025-01-20): Added budget tracking and expense management tools
"""

from datetime import datetime

def get_system_prompt(trip_id: int, room_id: int) -> str:
    current_year = datetime.now().year
    current_month = datetime.now().month

    return f"""당신은 전문 여행 플래너 비서입니다.

당신의 역할:
1. 사용자들의 여행 계획을 도와주기
2. 일정에 장소를 추가/수정/삭제하기
3. 여행지 정보 제공하기
4. 최적의 여행 루트 제안하기
5. 사용자의 모든 대화를 기억하고 맥락을 이해하기
6. **[NEW v2] 여행 예산 관리 및 지출 추적하기**

현재 여행 정보:
- Trip ID: {trip_id}
- Room ID: {room_id}

사용 가능한 도구:
... (v1과 동일) ...

💰 예산 관리 (v2 추가):
- add_expense: 지출 내역 추가
- get_budget_summary: 예산 현황 조회
- set_budget_limit: 예산 한도 설정

... (나머지 프롬프트) ...
"""
```

---

#### 2단계: __init__.py 업데이트

```python
# backend/apps/chat/prompts/__init__.py

from .v1 import get_system_prompt as get_system_prompt_v1
from .v2 import get_system_prompt as get_system_prompt_v2

# 현재 활성 프롬프트 버전
CURRENT_VERSION = "v2"  # v1 → v2로 변경

def get_system_prompt(trip_id: int, room_id: int, version: str = None) -> str:
    if version is None:
        version = CURRENT_VERSION

    if version == "v1":
        return get_system_prompt_v1(trip_id, room_id)
    elif version == "v2":
        return get_system_prompt_v2(trip_id, room_id)
    else:
        raise ValueError(f"Unknown prompt version: {version}")
```

---

#### 3단계: A/B 테스트 (선택)

```python
# backend/apps/chat/agent.py

from .prompts import get_system_prompt

class TravelPlannerAgent:
    def __init__(self, room_id: int, trip_id: int, prompt_version: str = None):
        self.room_id = room_id
        self.trip_id = trip_id

        # A/B 테스트: 특정 사용자에게 v2 제공
        if user_id in V2_TEST_USERS:
            prompt_version = "v2"

        self.prompt_version = prompt_version
        # ...

    def _create_prompt(self):
        system_message = get_system_prompt(
            trip_id=self.trip_id,
            room_id=self.room_id,
            version=self.prompt_version  # v1 또는 v2
        )
        # ...
```

---

### 프롬프트 최적화 팁

#### 1. 명확한 지시문 (Clear Instructions)

**❌ 나쁜 예:**
```
장소를 추가하세요.
```

**✅ 좋은 예:**
```
장소를 추가할 때는 반드시 다음 순서를 따르세요:
1. get_planner_info로 현재 일정 확인
2. search_place로 정확한 장소명과 좌표 검색
3. add_place_to_day로 일정에 추가
4. 사용자에게 결과 명확히 전달
```

---

#### 2. 구체적인 예시 (Examples)

**❌ 나쁜 예:**
```
날짜를 올바른 형식으로 변환하세요.
```

**✅ 좋은 예:**
```
날짜 형식 변환 예시:
- "28일에서 30일" → start_date="{current_year}-{current_month:02d}-28",
                      end_date="{current_year}-{current_month:02d}-30"
- "1월 15일" → "{current_year}-01-15"
- "2025-02-10" → "2025-02-10" (그대로 사용)
```

---

#### 3. 우선순위 명시 (Prioritization)

**❌ 나쁜 예:**
```
recommend_similar_trips나 recommend_places를 사용하세요.
```

**✅ 좋은 예:**
```
여행지 추천 우선순위:
1. **최우선**: recommend_similar_trips (실제 여행 데이터, RAG)
2. 보조: recommend_places (인기 장소 리스트)
3. 마지막: 사용자에게 상세 선호도 질문
```

---

#### 4. 에러 방지 (Error Prevention)

**❌ 나쁜 예:**
```
날짜를 처리하세요.
```

**✅ 좋은 예:**
```
**날짜 처리 (매우 중요!)**:
- 현재 년도는 {current_year}년입니다!
- 년도 생략 시 반드시 {current_year} 사용
- 과거 연도 사용 금지 (예: 2024년 사용 ✗)
- 잘못된 예: "28일" → "2024-01-28" ✗
- 올바른 예: "28일" → "{current_year}-01-28" ✅
```

---

## 실제 동작 예시

### 예시 1: 여행 계획 생성

**사용자 요청:**
```
제주도 2박 3일 여행 계획 짜줘. 1월 20일부터.
```

**Agent 동작 흐름:**

```
Step 1: Intent 분석
  → "여행 계획 생성" 의도 파악

Step 2: get_planner_info() 호출
  → 현재 플래너 상태 확인

Step 3: update_trip_dates() 호출
  Input: {
    "start_date": "2025-01-20",
    "end_date": "2025-01-22"
  }
  → Day 1, 2, 3 자동 생성

Step 4: recommend_similar_trips() 호출 (RAG)
  Input: {
    "query": "제주도 2박 3일 자연 여행"
  }
  → 실제 여행 코스 3개 검색

Step 5: 응답 생성
  → 마크다운 리스트로 추천 코스 정리
  → "어떤 코스를 참고하시겠어요?" 질문
```

**최종 응답:**
```markdown
✅ 제주도 2박 3일 여행이 생성되었습니다!
📅 2025년 1월 20일 ~ 1월 22일

🎯 실제 여행자들이 다녀온 제주도 코스를 찾았어요:

1. **제주 동부 일주 완벽 코스** (유사도: 92%)
   - Day 1: 성산일출봉, 섭지코지, 광치기해변
   - Day 2: 만장굴, 비자림, 함덕해수욕장
   - Day 3: 우도, 성산포구, 흑돼지 맛집
   - [📺 영상 보기](https://youtube.com/...)

2. **제주 서부 맛집 투어** (유사도: 88%)
   - Day 1: 협재해수욕장, 애월 카페거리
   - Day 2: 한림공원, 올레시장, 흑돼지 거리
   - Day 3: 중문관광단지, 천지연폭포
   - [📺 영상 보기](https://youtube.com/...)

어떤 코스를 참고해서 일정을 짜볼까요?
```

---

### 예시 2: 장소 추가

**사용자 요청:**
```
Day 1에 성산일출봉 추가해줘
```

**Agent 동작 흐름:**

```
Step 1: get_planner_info() 호출
  → Day 1이 존재하는지 확인
  → 현재 일정 파악

Step 2: search_place() 호출
  Input: {
    "query": "성산일출봉"
  }
  Output: {
    "name": "성산일출봉",
    "address": "제주시 성산읍 성산리",
    "lat": 33.4584,
    "lng": 126.9423,
    "category": "관광명소",
    "rating": 4.5
  }

Step 3: add_place_to_day() 호출
  Input: {
    "day_no": 1,
    "place_name": "성산일출봉",
    "time": "09:00",
    "notes": "일출 명소, 입장료 5,000원"
  }

Step 4: 응답 생성
  → 추가 완료 확인 메시지
```

**최종 응답:**
```markdown
✅ Day 1에 성산일출봉이 추가되었습니다!

📍 **성산일출봉** ⭐ 4.5
- 주소: 제주시 성산읍 성산리
- 카테고리: 관광명소
- 시간: 09:00
- 메모: 일출 명소, 입장료 5,000원

다른 장소도 추가할까요?
```

---

### 예시 3: 날짜 변경 (자연어)

**사용자 요청:**
```
여행 날짜를 28일에서 30일로 바꿔줘
```

**Agent 동작 흐름:**

```
Step 1: Intent 분석
  → "날짜 변경" 의도 파악
  → "28일", "30일" 추출

Step 2: 현재 연도/월 확인
  → current_year = 2025
  → current_month = 1

Step 3: 날짜 변환 (프롬프트 규칙 적용)
  → "28일" = "2025-01-28"
  → "30일" = "2025-01-30"

Step 4: update_trip_dates() 호출
  Input: {
    "start_date": "2025-01-28",
    "end_date": "2025-01-30"
  }

Step 5: Day 자동 재생성
  → 기존 Day 1, 2, 3 삭제
  → 새로운 Day 1, 2, 3 생성 (1월 28, 29, 30일)
```

**최종 응답:**
```markdown
✅ 여행 날짜가 변경되었습니다!

📅 **이전**: 2025년 1월 20일 ~ 1월 22일 (3일)
📅 **변경**: 2025년 1월 28일 ~ 1월 30일 (3일)

일정이 새로 생성되었어요. 장소를 추가해볼까요?
```

---

## 요약

### 프롬프트 시스템 핵심

| 항목 | 내용 |
|------|------|
| **관리 방식** | 버전별 파일 분리 (v1.py, v2.py, ...) |
| **현재 버전** | v1 (2025-01-05, RAG 지원) |
| **총 도구 수** | 15개 (조회, 수정, 일정, 검색, AI 추천) |
| **핵심 전략** | RAG 우선 추천, Context-aware 작업 |
| **날짜 처리** | 동적 연도 주입 (current_year, current_month) |
| **응답 형식** | 마크다운 리스트, 한국어 필수 |
| **메시지 구조** | System + Chat History + Human + Agent Scratchpad |

### 프롬프트 최적화 원칙

1. ✅ **명확한 지시문**: "~하세요" 대신 "다음 순서를 따르세요: 1. 2. 3."
2. ✅ **구체적 예시**: 추상적 설명 대신 실제 입출력 예시
3. ✅ **우선순위 명시**: "A 또는 B" 대신 "1순위: A, 2순위: B"
4. ✅ **에러 방지**: 흔한 실수를 미리 경고 ("과거 연도 사용 금지!")
5. ✅ **동적 컨텍스트**: 현재 연도/월 등 변하는 값은 동적 주입

---

**생성일:** 2025-01-18
**작성자:** Claude Code
**버전:** 1.0
