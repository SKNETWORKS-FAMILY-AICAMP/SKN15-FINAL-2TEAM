# 일정 장소 추가 검증 프로세스 분석

**작성일**: 2025-01-07
**분석 대상**: Agent의 `add_place_to_day` 도구와 장소 검색/검증 프로세스

---

## 📋 목차
1. [현재 상태 분석](#현재-상태-분석)
2. [문제점 식별](#문제점-식별)
3. [TripItem 데이터 구조](#tripitem-데이터-구조)
4. [개선 방안](#개선-방안)
5. [구현 옵션 비교](#구현-옵션-비교)
6. [권장 솔루션](#권장-솔루션)

---

## 현재 상태 분석

### 1. 사용자 질문
> "일정에 추가해줄때는 지도에 검색해서 있는 데이터를 추가하는거야?"

### 2. 현재 동작 방식

**Agent의 `add_place_to_day` 도구** ([agent.py:126-164](backend/apps/chat/agent.py#L126-L164))

```python
@tool
def add_place_to_day(day_no: int, place_name: str, time: str = "09:00", notes: str = "") -> str:
    """특정 일차에 장소를 추가합니다."""

    # ⚠️ 문제: place_name을 텍스트로만 받아서 그대로 저장
    TripItem.objects.create(
        day_idx=day,
        item_type='custom',
        title=place_name,        # ← 단순 텍스트만 저장
        start_time=time,
        notes=notes,
        order_in_day=order,
        lock_flag=False
    )

    return f"✅ Day {day_no}에 '{place_name}'을(를) 추가했습니다"
```

**핵심 문제**:
- `place_name`을 **검증 없이** 그대로 `title` 필드에 저장
- `place_idx` (Place 테이블 FK)는 **NULL로 남김**
- 좌표, 주소, 카테고리 등 **메타데이터 손실**

---

## 문제점 식별

### ❌ 현재 동작의 문제점

| 문제 | 설명 | 영향 |
|------|------|------|
| **검증 누락** | 사용자가 "경복궁"이라고 입력해도 실제로 존재하는 장소인지 확인하지 않음 | 오타, 잘못된 장소명 저장 가능 |
| **메타데이터 부족** | 좌표, 주소, 평점, 전화번호 등 정보가 저장되지 않음 | 지도 표시 불가, 상세 정보 제공 불가 |
| **중복 가능성** | "경복궁", "경복궁역", "Gyeongbokgung" 등이 모두 다른 장소로 저장됨 | 데이터 품질 저하 |
| **FK 미연결** | `place_idx`가 NULL이므로 Place 테이블과 연결되지 않음 | 관계형 데이터의 장점 활용 불가 |
| **통계 불가** | 어떤 장소가 가장 많이 선택되는지 집계 어려움 | 인기 장소 분석 불가 |

### 🔍 Agent가 가진 검색 도구

**`search_place` 도구는 이미 존재** ([agent.py:167-246](backend/apps/chat/agent.py#L167-L246))

```python
@tool
def search_place(keyword: str, region: str = None) -> str:
    """장소를 검색합니다. 내부 DB와 카카오맵 양쪽을 검색하여 풍부한 정보를 제공합니다."""

    results = []

    # 1. 내부 DB 검색 (Place 테이블)
    db_places = Place.objects.filter(
        Q(name__icontains=keyword) | Q(ko_name__icontains=keyword)
    )[:3]

    for place in db_places:
        results.append({
            "source": "DB",
            "name": place.ko_name or place.name,
            "address": place.address,
            "latitude": float(place.latitude),
            "longitude": float(place.longitude),
            "rating": float(place.rating),
            "reviews": place.user_ratings_total,
            "phone": place.phone_number,
            "website": place.website,
            # ... 더 많은 정보
        })

    # 2. 카카오맵 API 검색 (최신 정보)
    response = requests.get("https://dapi.kakao.com/v2/local/search/keyword.json", ...)
    # ... 카카오 검색 결과 추가

    return json.dumps(results, ensure_ascii=False, indent=2)
```

**⚠️ 문제: 검색 도구는 있지만 일정 추가 전에 필수로 사용하지 않음!**

---

## TripItem 데이터 구조

### TripItem 모델 분석 ([models.py:165-209](backend/apps/plans/models.py#L165-L209))

```python
class TripItem(models.Model):
    """일정 항목 모델 - 여행 일차별 활동/장소"""

    item_idx = models.AutoField(primary_key=True)
    day_idx = models.ForeignKey(TripDay, on_delete=models.CASCADE)
    item_type = models.CharField(max_length=20, choices=ITEM_TYPE_CHOICES)

    # 🔑 핵심: Place 테이블과의 연결 (현재 사용 안 됨)
    place_idx = models.ForeignKey(
        Place,
        on_delete=models.SET_NULL,
        null=True,  # ← NULL 허용 (선택적)
        blank=True,
        related_name='trip_items'
    )

    # 📝 현재는 여기에만 텍스트 저장
    title = models.TextField(null=True, blank=True)

    start_time = models.TimeField(null=True, blank=True)
    end_time = models.TimeField(null=True, blank=True)
    estimated_cost = models.DecimalField(...)
    lock_flag = models.BooleanField(default=False)
    notes = models.TextField(null=True, blank=True)
    order_in_day = models.IntegerField()

    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

### 현재 vs 이상적인 데이터 저장 방식

| 필드 | 현재 상태 | 이상적인 상태 |
|------|-----------|---------------|
| `place_idx` | **NULL** | Place 객체의 PK (예: 1234) |
| `title` | "경복궁" (단순 텍스트) | "경복궁" 또는 Place.ko_name |
| 좌표 | ❌ 없음 | Place.latitude, Place.longitude |
| 주소 | ❌ 없음 | Place.address |
| 평점 | ❌ 없음 | Place.rating |
| 전화번호 | ❌ 없음 | Place.phone_number |

**💡 모델은 이미 FK 연결을 지원하지만, Agent가 활용하지 않음!**

---

## 개선 방안

### 옵션 1: 프롬프트 개선 (Easy) ⭐ **권장**

**아이디어**: Prompt에 규칙 추가하여 Agent가 자동으로 검색 후 추가하도록 유도

**구현**:

[backend/apps/chat/prompts/v1.py](backend/apps/chat/prompts/v1.py) 수정

```python
def get_system_prompt(trip_id: int, room_id: int) -> str:
    return f"""당신은 전문 여행 플래너 비서입니다.

중요한 규칙:
1. 항상 먼저 get_planner_info로 현재 일정을 확인하세요
2. **장소를 추가할 때는 반드시 다음 순서를 따르세요:**
   a. search_place로 정확한 장소명, 주소, 좌표를 확인
   b. 검색 결과에서 가장 적합한 장소 선택
   c. add_place_to_day로 추가 (검색된 정확한 이름 사용)
3. 사용자가 특정 장소 근처의 추천을 원하면 search_nearby를 사용하세요
4. 지역별 인기 장소를 추천할 때는 recommend_places를 사용하세요
...

**장소 추가 워크플로우 (매우 중요!):**
사용자가 "경복궁 추가해줘", "Day 1에 남산타워 넣어줘" 같은 요청을 하면:
1. ✅ 먼저 search_place(keyword="경복궁") 실행
2. ✅ 검색 결과에서 정확한 이름 확인 (예: "경복궁(景福宮)")
3. ✅ add_place_to_day(day_no=1, place_name="경복궁(景福宮)", ...)
4. ✅ 사용자에게 "경복궁(景福宮)을 Day 1에 추가했습니다 (주소: 서울특별시 종로구...)" 형식으로 응답

**절대 하지 말 것:**
- ❌ search_place 없이 바로 add_place_to_day 호출 금지
- ❌ 사용자가 입력한 텍스트를 검증 없이 그대로 추가 금지
"""
```

**장점**:
- ✅ 코드 수정 최소화 (프롬프트만 수정)
- ✅ 즉시 배포 가능
- ✅ Agent가 자동으로 검증 프로세스 수행

**단점**:
- ⚠️ Agent가 규칙을 무시할 가능성 있음 (GPT-4의 한계)
- ⚠️ 여전히 `place_idx`는 NULL로 저장됨

---

### 옵션 2: 도구 통합 (Medium)

**아이디어**: `add_place_to_day` 도구 내부에서 자동으로 `search_place` 호출

**구현**:

[backend/apps/chat/agent.py](backend/apps/chat/agent.py) 수정

```python
@tool
def add_place_to_day(day_no: int, place_name: str, time: str = "09:00", notes: str = "") -> str:
    """특정 일차에 장소를 추가합니다. (자동 검증 포함)"""
    from apps.plans.models import TripDay, TripItem
    from apps.places.models import Place
    import requests

    try:
        day = TripDay.objects.get(trip_idx=trip_id, day_no=day_no)

        # 🆕 1. 장소 검증 및 검색
        verified_place = None
        verified_name = place_name

        # 내부 DB에서 먼저 검색
        db_place = Place.objects.filter(
            Q(name__icontains=place_name) | Q(ko_name__icontains=place_name)
        ).first()

        if db_place:
            verified_place = db_place
            verified_name = db_place.ko_name or db_place.name
            logger.info(f"✅ Found place in DB: {verified_name} (ID: {db_place.place_idx})")
        else:
            # 카카오맵에서 검색
            url = "https://dapi.kakao.com/v2/local/search/keyword.json"
            response = requests.get(url, headers=headers, params={"query": place_name})
            data = response.json()

            if data.get('documents'):
                verified_name = data['documents'][0]['place_name']
                logger.info(f"✅ Found place in Kakao: {verified_name}")

        # 🆕 2. TripItem 생성 (place_idx 연결)
        last_item = TripItem.objects.filter(day_idx=day.day_idx).order_by('-order_in_day').first()
        order = (last_item.order_in_day + 1) if last_item else 1

        item = TripItem.objects.create(
            day_idx=day,
            item_type='place' if verified_place else 'custom',  # DB 연결 시 'place'
            place_idx=verified_place,  # ← FK 연결!
            title=verified_name,       # ← 검증된 이름
            start_time=time,
            notes=notes,
            order_in_day=order,
            lock_flag=False
        )

        # 🆕 3. 상세 정보 포함한 응답
        response_msg = f"✅ Day {day_no}에 '{verified_name}'을(를) 추가했습니다 (시간: {time})"
        if verified_place:
            response_msg += f"\n📍 주소: {verified_place.address}"
            if verified_place.rating:
                response_msg += f"\n⭐ 평점: {verified_place.rating} ({verified_place.user_ratings_total}개 리뷰)"

        logger.info(f"✅ Added place: {verified_name} (place_idx: {verified_place.place_idx if verified_place else 'NULL'})")
        return response_msg

    except TripDay.DoesNotExist:
        return f"❌ Day {day_no}을(를) 찾을 수 없습니다."
    except Exception as e:
        logger.error(f"❌ add_place_to_day error: {e}")
        return f"❌ 추가 실패: {str(e)}"
```

**장점**:
- ✅ Agent의 행동과 무관하게 항상 검증됨
- ✅ `place_idx` FK 연결로 데이터 품질 향상
- ✅ 사용자에게 더 풍부한 정보 제공

**단점**:
- ⚠️ 도구 내부에서 API 호출 (성능 영향)
- ⚠️ 에러 처리 복잡도 증가

---

### 옵션 3: 새로운 통합 도구 (Advanced)

**아이디어**: `search_and_add_place` 도구를 새로 만들어 검색과 추가를 원자적으로 처리

**구현**:

```python
@tool
def search_and_add_place(day_no: int, keyword: str, time: str = "09:00", notes: str = "") -> str:
    """장소를 검색하고 일정에 추가합니다. (검증 + 추가 통합)

    Args:
        day_no: 추가할 일차
        keyword: 검색할 장소명
        time: 방문 시간
        notes: 메모

    Returns:
        검색 결과 + 추가 결과
    """
    # 1. 검색
    search_results = search_place(keyword)

    # 2. 가장 적합한 결과 선택 (첫 번째)
    results = json.loads(search_results)
    if not results:
        return f"❌ '{keyword}' 검색 결과가 없습니다."

    best_match = results[0]

    # 3. DB에서 Place 객체 찾기 또는 생성
    place, created = Place.objects.get_or_create(
        name=best_match['name'],
        defaults={
            'ko_name': best_match.get('name'),
            'address': best_match.get('address'),
            'latitude': best_match.get('latitude'),
            'longitude': best_match.get('longitude'),
            'rating': best_match.get('rating'),
            # ...
        }
    )

    # 4. TripItem 추가
    day = TripDay.objects.get(trip_idx=trip_id, day_no=day_no)
    TripItem.objects.create(
        day_idx=day,
        item_type='place',
        place_idx=place,  # FK 연결
        title=place.ko_name or place.name,
        start_time=time,
        notes=notes,
        order_in_day=get_next_order(day),
        lock_flag=False
    )

    return f"""✅ '{place.ko_name or place.name}'을(를) Day {day_no}에 추가했습니다.
📍 주소: {place.address}
⭐ 평점: {place.rating} ({place.user_ratings_total}개 리뷰)
🔗 place_idx: {place.place_idx} (DB 연결됨)
"""
```

**Prompt 수정**:

```python
사용 가능한 도구:
...
🆕 📍 일정 추가 (통합):
- search_and_add_place: 장소를 검색하고 일정에 추가 (원자적 작업)
  * 자동으로 검증하고 DB와 연결
  * 사용자가 "경복궁 추가해줘" 같은 요청 시 이 도구 사용
  * add_place_to_day 대신 이 도구를 우선 사용하세요
...
```

**장점**:
- ✅ 검색 → 검증 → 추가가 원자적으로 처리
- ✅ Agent의 실수 가능성 최소화
- ✅ 데이터 품질 최상
- ✅ Place 테이블에 없는 장소도 자동 추가

**단점**:
- ⚠️ 새로운 도구 추가로 복잡도 증가
- ⚠️ 기존 `add_place_to_day` 도구와 중복 가능성
- ⚠️ Agent가 어떤 도구를 써야 할지 혼란 가능

---

## 구현 옵션 비교

| 기준 | 옵션 1: 프롬프트 개선 | 옵션 2: 도구 통합 | 옵션 3: 새 도구 |
|------|----------------------|------------------|----------------|
| **구현 난이도** | ⭐ Easy | ⭐⭐ Medium | ⭐⭐⭐ Advanced |
| **코드 수정량** | 최소 (프롬프트만) | 중간 (도구 1개) | 많음 (도구 추가) |
| **검증 신뢰성** | 70% (Agent 의존) | 100% (강제) | 100% (강제) |
| **place_idx 연결** | ❌ 안 됨 | ✅ 가능 | ✅ 가능 |
| **성능 영향** | 없음 | 약간 (API 호출) | 약간 (API 호출) |
| **유지보수성** | 높음 | 중간 | 낮음 (복잡) |
| **즉시 배포** | ✅ 가능 | ⚠️ 테스트 필요 | ⚠️ 테스트 필요 |

---

## 권장 솔루션

### 🎯 2단계 접근법 (Hybrid)

#### Phase 1: 프롬프트 개선 (즉시 배포)
- **옵션 1**을 먼저 적용하여 즉시 개선 효과 확인
- Agent가 70-80% 이상 자동으로 검색 → 추가 워크플로우를 따르도록 유도
- 배포 리스크 최소화

#### Phase 2: 도구 통합 (장기 개선)
- **옵션 2** 또는 **옵션 3** 적용하여 100% 보장
- 데이터 품질 완벽 보장
- 통계 분석, 인기 장소 추출 등 고급 기능 활성화

### 구체적 구현 계획

**Step 1: 프롬프트 v1.1 배포** (1일)
```python
# backend/apps/chat/prompts/v1.py 수정
# - 장소 추가 워크플로우 규칙 추가
# - 예시 추가하여 Agent 학습 유도
```

**Step 2: 모니터링 및 분석** (1주)
```python
# 로그 분석:
# - Agent가 search_place → add_place_to_day 순서를 따르는 비율
# - place_name과 실제 검색 결과의 일치율
# - 사용자 피드백 수집
```

**Step 3: 도구 개선 (옵션 선택)** (3일)
- 모니터링 결과에 따라 옵션 2 또는 옵션 3 선택
- A/B 테스트로 효과 검증

### 예상 효과

| 지표 | 현재 | Phase 1 후 | Phase 2 후 |
|------|------|------------|------------|
| 검증률 | 0% | 70-80% | 100% |
| place_idx 연결 | 0% | 0% | 90%+ |
| 데이터 품질 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 사용자 만족도 | 보통 | 높음 | 매우 높음 |

---

## 부록: 실제 사용 시나리오

### 현재 동작

```
User: "Day 1에 경복궁 추가해줘"

Agent: [직접 add_place_to_day 호출]
TripItem.objects.create(
    title="경복궁",  # ← 검증 없이 저장
    place_idx=None   # ← FK 연결 안 됨
)

Response: "✅ Day 1에 '경복궁'을 추가했습니다"
```

### 개선 후 동작 (Phase 1)

```
User: "Day 1에 경복궁 추가해줘"

Agent: [프롬프트 규칙에 따라 자동 검색]
1. search_place(keyword="경복궁")
   → 결과: [{"name": "경복궁(景福宮)", "address": "서울특별시 종로구...", "rating": 4.6}]

2. add_place_to_day(day_no=1, place_name="경복궁(景福宮)")
   → TripItem.objects.create(title="경복궁(景福宮)", ...)

Response: "✅ Day 1에 '경복궁(景福宮)'을 추가했습니다
📍 주소: 서울특별시 종로구 사직로 161
⭐ 평점: 4.6점 (12,543개 리뷰)"
```

### 개선 후 동작 (Phase 2)

```
User: "Day 1에 경복궁 추가해줘"

Agent: [도구가 자동으로 검증]
search_and_add_place(day_no=1, keyword="경복궁")

내부 처리:
1. search_place() 자동 호출
2. Place 테이블에서 "경복궁(景福宮)" 찾기 (place_idx=1234)
3. TripItem.objects.create(
     place_idx=1234,  # ← FK 연결!
     title="경복궁(景福宮)",
     ...
   )

Response: "✅ Day 1에 '경복궁(景福宮)'을 추가했습니다
📍 주소: 서울특별시 종로구 사직로 161
⭐ 평점: 4.6점 (12,543개 리뷰)
🔗 장소 ID: 1234 (데이터베이스 연결됨)"
```

---

## 결론

**질문**: "일정에 추가해줄때는 지도에 검색해서 있는 데이터를 추가하는거야?"

**답변**:
- **현재**: ❌ 아니오. 사용자가 입력한 텍스트를 검증 없이 그대로 저장합니다.
- **개선 후**: ✅ 예. 지도(DB + 카카오맵) 검색으로 검증된 데이터를 추가합니다.

**권장 조치**:
1. ✅ 즉시: **프롬프트 개선** (v1 → v1.1)으로 Agent가 자동으로 검색하도록 유도
2. ✅ 장기: **도구 통합** (옵션 2 또는 3)으로 100% 검증 보장

이렇게 하면 데이터 품질이 크게 향상되고, 지도 표시, 통계 분석, 인기 장소 추천 등 고급 기능을 활용할 수 있습니다.
